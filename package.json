{
  "name": "agent-workflow",
  "version": "1.4.2",
  "private": true,
  "type": "module",
  "description": "Assisted workflow procedures, templates, approval and readiness gates, traceability, lifecycle validation, and regression fixtures",
  "scripts": {
    "test": "node --test validator/test/*.test.js",
    "validate": "node validator/cli.js"
  },
  "engines": {
    "node": ">=22"
  }
}
