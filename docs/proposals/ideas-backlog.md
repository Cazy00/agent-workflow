# Owner ideas backlog

**Status of this file:** the owner's list of ideas, recorded 2026-09-19. On 2026-09-21 the owner authorised the four selected additions. They were implemented in task T-0003 and released in `v1.1.0`. On 2026-09-25 the owner added three ideas, two from a real adoption attempt and one about oversight, recorded as IDEA-06 to IDEA-08; they are unevaluated. Later the same day the owner asked for a comparison with the "Super System" template, recorded as IDEA-09 with a source note; it is unevaluated. That evening the owner reviewed an A-to-Z walkthrough of the workflow and gave feedback on autonomy of the verification layers, tests, the signing key, budgets, an owner inbox, a scripted setup and mid-project changes, recorded as IDEA-10; the parts needing no owner decision were implemented the same day and released in `v1.1.0`, with the backlog pass that followed. Later that night the owner asked for the whole backlog to be studied and the worthwhile parts implemented; IDEA-06 to IDEA-09 now carry verdicts, implementation notes and reasons for what was not done. Afterwards the owner asked whether the workflow enforces good engineering across a codebase as large as ERPNext or Odoo while the owner oversees rather than implements, recorded as IDEA-11; the changes agreed after a consultation with Fable were implemented the same day and released in `v1.2.0`. The owner then asked to see the progress view in GitHub, recorded as IDEA-12, implemented the same day and released in `v1.2.0`. Later that night the owner asked whether two people, each with their own agents, can plan for each other in one repository, recorded as IDEA-13; the changes agreed after a consultation with Fable were implemented the same night and released in `v1.3.0`. On 2026-09-26 the owner asked how milestones are cut and planned and what happens to work found along the way, recorded as IDEA-14; the changes agreed after a consultation with Fable were implemented the same day, unreleased. The same day the owner asked for the defect IDEA-13 noticed, a removed task record that no longer satisfies another task's prerequisite, to be fixed, recorded as IDEA-15 and combined with IDEA-14's timing; the fix agreed with Fable was implemented the same day and is unreleased. Afterwards the owner asked whether the growing corpus is a good thing and asked for it to be recorded for possible cost optimisation, recorded as IDEA-16; evaluated the same day after a consultation with Fable, with a structural pass drafted and unreleased. Original ideas and first-read notes below are historical, not operating instructions.
**Audience:** the agent asked to evaluate these ideas. For each idea, decide whether it adds value, and if it does, write an implementation plan.
**Authority:** none. `POLICY.md` still governs. An adopted idea goes through `procedures/maintenance.md`: classify it as an *improvement proposal*, then get independent review, owner approval, a new workflow version and explicit project adoption. A working agent must not weaken its own rules, and an idea written here does not count as permission to do so.

**Evaluation:** all five groups were assessed on 2026-09-21, and a Codex/Fable discussion settled what to include, defer or pass (see [Settled: defer or pass](#settled-defer-or-pass)). Those conclusions supersede the unverified first-read notes below where they differ. The original ideas and quotations are preserved; the status lines distinguish local changes from release and project adoption. IDEA-06 to IDEA-09 are not covered by that evaluation.

## Settled: defer or pass

| Decision | Items | Reason |
|---|---|---|
| **Pass on for this package** | Full ECC installation; copied common rule packs/hooks; automatic merging or learned-rule adoption | No demonstrated incremental benefit here, substantial inspection/maintenance burden, and defaults that do not fit our authority and risk-based checks. ECC can remain reference material. |
| **Defer until a concrete task needs them** | Individual adapted ECC assets, installed TDD/domain-modeling skill adoption, model routing, stack-specific MCP bundles, an extra test designer | Select, vet, pin, and trial the smallest useful tool when there is a real need. No automatic toolbox registration. A separate test designer is an option for consequential work, not a standing team member. |
| **Do not add** | Universal TDD/coverage quotas; a new `technique` or `out_of_bounds` schema field; a separate techniques document; compulsory prompt rewriting; routine approval for every reversible detail; blanket dependency bans; unconditional rollback | These duplicate current mechanisms, overconstrain delegated work, or create process cost without established benefit. Dependency choices still follow project policy, task scope, and consequential-decision rules. |

## How to evaluate each idea

1. Read the idea, including the owner's own words in the quote.
2. Check what the workflow already covers. The coverage notes below are the note-taker's first read and have **not** been verified, so check them against `POLICY.md`, `procedures/` and `templates/`.
3. Decide: adopt, adopt in part, reject, or needs owner decision. Give evidence for the decision, following POLICY's approach of judging by recurrence, severity, wasted effort and observed benefit.
4. When you adopt something, propose the smallest change that works (instructions, a template, config, the validator, or a fixture). Show that existing safeguards still hold.
5. Update this file's **Status** line for the idea, and link any resulting maintenance record, decision or plan.

Status values: `Unevaluated` · `Under evaluation` · `Adopted → <link>` · `Partly adopted → <link>` · `Rejected (reason)` · `Needs owner decision` · `Implemented (unreleased)` · `Partly implemented (unreleased)` · `Implemented, released in <tag>` · `Partly implemented, released in <tag>` · `Deferred`.

---

## IDEA-01 — Evaluate ECC (`https://github.com/affaan-m/ECC`)

**Status:** Deferred — no ECC bundle installation or asset adoption in this package; individual assets need a concrete task and vetting. See [Settled: defer or pass](#settled-defer-or-pass).

**Owner's question (verbatim):**
> how can we use https://github.com/affaan-m/ECC.git ?? is it good as thay are saying ?? what can we do with it ?? or its just a hype ?

**Restated:** The owner has seen this repository being promoted and wants an honest assessment. What is it? Does it work as claimed? Is any part of it useful for this workflow or for projects that use this workflow? Or is it mostly hype?

**The note-taker did not research it.** The note-taker has no view on the repo's contents or quality. Start from the source itself, not from its README or from social media claims.

**Questions to answer:**
- What does ECC actually contain: skills, agents, hooks, rules, commands, MCP configs, scripts? Which harness does it target (Claude Code, Codex, others)?
- Which claims can be checked, and which are marketing? Is there evidence of effect, such as benchmarks, tests or real-world reports, or only assertions?
- How does it overlap with what this workflow already provides: readiness gates, independent review, durable records, the validator? Does any part of it conflict with `POLICY.md`? Example: anything that grants autonomy the policy reserves, auto-approves work, or blurs the owner and worker identities.
- Is there a small piece worth taking (a single skill, a hook, a checklist) as opposed to installing the whole thing?
- Maintenance and supply-chain risk: licence, activity, how much it pulls in, whether scripts run on install.

**Constraints that already apply:** POLICY § *Skills and reusable tools* says that before adopting an external skill or tool you must: inspect its instructions, scripts, permissions and dependencies; pin a version; test it on representative work; and record its purpose and limitations. Anything adopted is registered in `templates/toolbox.md`. Skills never grant extra authority.

**Expected output:** a short verdict (use / take specific parts / do not use) with evidence, plus the list of candidate parts if any.

---

## IDEA-02 — Let the agent open, log into and look at the running app itself, then iterate

**Status:** Implemented, released in `v1.1.0` — running-app verification with authorised test accounts/data and existing safeguards. Implemented in T-0003.

**Owner's words (verbatim):**
> Is there any rule in the current agent that prohibits the agent from actually open and seize the output itself? If there is, let's remove this policy. As long the app have demo credentials or it have credentials that have been provided by me, the agent should be able to log in and actually test and see the output by itself.
>
> And actually make corrections and go with iterations if it wants to do so in order to get the task or the work done.

**Restated:** The owner wants the implementing agent to verify its own work visually and interactively, and to fix problems in a loop. That means starting the app, opening it in a browser (or a simulator), logging in with demo or test credentials, or credentials the owner supplied, then going through the flows, looking at the result, fixing what is wrong, and repeating until the task works. Where a rule blocks this, the owner wants it removed.

**Note-taker's first read of the current rules (verify these):**
- A grep of `POLICY.md`, `procedures/` and `templates/` found **no workflow rule that forbids the agent from running the app, opening it in a browser or looking at its output**. The rules point the other way:
  - POLICY § 9 and `procedures/execute.md` already require "integration/journey" checks, plus "performance/accessibility/device" checks where they apply.
  - POLICY § 9 calls self-verification "useful evidence".
  - `procedures/execute.md` allows repeat attempts until they stop making progress. Its stop condition is "repeated no-progress attempts", not "one try".
- **Rules nearby that the idea must not break:**
  - POLICY § 9 (≈ line 566): "Candidate code must not run with owner credentials or privileges that allow it to approve itself." Logging into the app under test with the owner's *real* account could conflict with this. Demo or test accounts would not.
  - POLICY ≈ line 336 and `procedures/identity.md`: credentials go in secret storage, never in repository files, task records or transcripts.
  - POLICY ≈ line 349: when worker credentials fail, never fall back to the owner account.
  - POLICY § 9: self-verification does **not** replace independent review, integration verification or owner acceptance. The idea should be adopted as *stronger implementer verification*, not as a way around the other layers.
- **The likely real blocker is outside this repository.** The agent harness (for example, Claude Code's built-in browser tools) has its own safety rules: it will not type passwords into login forms or create accounts, whatever the user or the repo says. That is probably why the agent "couldn't log in", not a rule in this workflow. **This repo cannot turn that rule off.** The evaluator should confirm this. If it holds, look at approaches that don't need the agent to type a password, for example:
  - a dev/test-only auth bypass or seeded session, switched on only in local/test environments;
  - automated browser tests (such as Playwright) that read test credentials from environment variables and save a logged-in session state, which the agent then reuses;
  - pre-created demo accounts that the owner logs into once, sharing the resulting session;
  - opening protected pages with a test token instead of the login form.

**Questions to answer:**
- Is there any wording in this repo, or in how projects adopt it, that agents *read* as forbidding self-testing, even though it doesn't? If so, clarify it.
- Should the workflow **require** a "run it and look" step for UI-affecting tasks, with screenshots or journey results recorded as evidence in the task checkpoint?
- Where do demo or test credentials belong: in the profile (`templates/profile.md`) as a reference to secret storage, never as values? What makes a credential "safe for the agent" (test-only, no owner privileges, no approval power)?
- How should the iterate-until-it-works loop be bounded? The existing limits on retries, usage and spending, and the "no meaningful progress" stop rule, look like the right mechanism. Confirm this.

**Expected output:** either a clarification or a change to implementer verification (instructions, template fields, maybe a fixture), plus a clear statement of what the harness blocks and the recommended workaround.

---

## IDEA-03 — Lessons from YouTube video A ("tips after hundreds of hours coding with AI", sponsored by JetBrains Junie)

**Status:** Partly implemented, released in `v1.1.0` — optional references and exclusions in the existing task brief; most advice was already covered. Implemented in T-0003; see [Settled: defer or pass](#settled-defer-or-pass) for the rest.

The owner shared a transcript and asked "what can we take from it?". Below is the note-taker's summary of the video's claims, each with a first guess at where this workflow already stands. The evaluator decides what, if anything, is worth adding.

| # | The video's claim (summarised) | First guess at existing coverage | Possible gap to evaluate |
|---|---|---|---|
| A1 | The person using AI must already know how to program. AI multiplies existing skill. | Not a workflow concern. The owner/agent split is defined in POLICY § 1. | Probably none. |
| A2 | Be as specific as possible. Vague prompts produce bad or "catfish" code (looks fine, rotten underneath). The video compares three prompt levels (bare, non-technical, fully technical), and only the fully technical one ran first time. | Readiness gates (`procedures/readiness.md`) block work until requirements, acceptance examples and decisions exist. | Is there a standard **task brief** telling the agent the exact stack, files, commands and expected behaviour? Or does it rebuild this from records every time? |
| A3–A5 | Attach documentation, screenshots or mock-ups, and reference links. Let the agent fetch docs, preferring `llms.txt` pages. | Profile has "visual direction". Design records exist. | Should task or feature records have a place for **reference material** (doc URLs, `llms.txt`, screenshots, mock-ups)? |
| — | Tip: write a short but technically complete prompt, then ask the AI to improve it using LLM prompting best practices. | Not covered. | Probably low value for this workflow. Evaluate briefly. |
| A6 | Smaller tasks give better results. If you can't break a task down, you don't understand it yet. | Milestones → tasks already exist, and a milestone is "one coherent journey". | Is there a size guideline for a single task? |
| A7 | Don't let the AI do all the thinking. The human owns the solution design. | The owner reserves decisions. Design records exist for consequential changes. | Probably covered. |
| A8 | Prompt pattern: **Task / Background info / "Do not" section**, where the last lists what must not be touched and the only files that may change. | Task records have `scope: []`. There is no explicit "must not touch" or "out of bounds" field. | **Worth evaluating:** an explicit *Do-not / out-of-bounds* section in `templates/task.md`. |
| A9 | Persistent project memory: an `AGENTS.md` or guidelines file with project summary, stack, commands and workflow. Separate rule files per technology if needed. | `AGENTS.md`, `CLAUDE.md`, profile and procedures. | Do adopting projects get guidance to keep **stack, commands and conventions** in their short repo guide? Should rule files per area or per sub-module be allowed? |
| A10 | Use MCP tools: docs fetchers (Context7), framework dev-tool MCPs (Next.js), browser DevTools MCP (console, network, layout shifts, performance). | POLICY § *Skills and reusable tools* plus `templates/toolbox.md` (vetting and registration). | Should the profile or toolbox suggest a **recommended verification toolset** per stack? Links to IDEA-02. |
| A11 | Always give the AI a way to verify its work: tests, running the app, CLI, CI. Check that AI-written tests actually test something. | POLICY § 9 (four verification layers, test fidelity in review, acceptance-ID traceability). | Mostly covered. Links to IDEA-02 for the "run the app and look" part. |

---

## IDEA-04 — Lessons from YouTube video B ("code with AI, not vibe code", sponsored by Warp)

**Status:** Partly implemented, released in `v1.1.0` — starting read hints and preserved behaviour, without new approval stops or fixed agent teams. Implemented in T-0003; see [Settled: defer or pass](#settled-defer-or-pass) for the rest.

The owner shared this transcript with the same question. Note-taker's summary:

| # | The video's claim (summarised) | First guess at existing coverage | Possible gap to evaluate |
|---|---|---|---|
| B1 | Set the foundation once: index the codebase, plus **global rules** (personal standards for every project) and **project rules** (stack, schema, API patterns, branch naming). You can add extra rule files for sub-modules. | Project level: `AGENTS.md` / `CLAUDE.md` / profile. Global level: the owner's `~/.claude/CLAUDE.md`, outside this repo. | Should the workflow say what goes in global versus project instructions, and allow scoped rule files per sub-module? |
| B2 | Be concrete. Use a prompt template: *Implement {thing} to {goal} using {lib}. Work in {paths} only. Respect {style/tests/rules}. Provide {tests/docs/migration}. If assumptions are needed, list them first.* | Task record has objective, scope, assumptions and verification. | Compare the task template field by field. Is "list assumptions **before** starting" enforced? Is "work in these paths only" enforced? (The validator may already check scope. Verify.) |
| B3 | Point the agent at the specific files to read or change. This improves accuracy and saves tokens compared with letting it search. | `scope: []` on tasks. | Should the task list **files to read** as well as files to change? |
| B4 | Choose models on purpose (expensive vs cheap). Use a strong reasoning model to plan and a coding model to implement. Don't switch models mid-conversation without reason, because it drops the cache. Keep conversations short, one per task, because history makes each turn cost more. | Profile records usage and spending limits. POLICY says fresh sessions recover from records. | Should the profile or procedures recommend a **model per role** (planner / implementer / reviewer) and session length? Measure cost before adding rules. |
| B5 | Plan first ("don't write code yet"), then implement exactly that plan with restrictions (listed files only, no new dependencies, keep existing behaviour). **If anything is unclear, stop and give two options with trade-offs instead of guessing.** | Readiness, reserved decisions, `templates/decision.md`. Agents must not self-resolve reserved decisions (fixture 03). | Check that the "present options, don't guess" behaviour is also required for *non-reserved* ambiguity found during implementation. Is "no new dependencies unless approved" a default? |
| B6 | Review AI output like pair-programming with a junior. Ask why a pattern was chosen, what the error cases are, and what it costs in performance. The human should read and edit code too. | Independent review in a separate context (`procedures/review.md`). | Probably covered for agents. Human code reading is an owner choice (acceptance explicitly isn't a code audit). |
| B7 | Several agents on one task: one writes code, one reviews, one writes tests, optionally one refactors from the feedback. Separate contexts avoid confirmation bias. Use git worktrees for parallel independent tasks. Two or three agents at most, or it gets overwhelming. | One coordinator by default. Independent reviewer in a separate context. Worktrees for parallel tasks (POLICY § 8, `procedures/execute.md`). | Is a **separate test-writer agent** (tests written without seeing the implementation's reasoning) worth adding? Evaluate against its cost. |
| B8 | Git through natural language. Screenshots for UI bugs. Docs URLs for APIs (whole pages cost tokens). Use AI to explore unfamiliar codebases and draw flowcharts for onboarding. | Graphify and similar tools are deferred (POLICY § *Optional navigation tools*). | Probably nothing new. Possibly an onboarding note. |
| B9 | Set permission tiers: some actions need approval, others run on their own. | Profile authority, reserved decisions, identity separation, approval receipts. | Probably covered. Check that the harness-level permission settings match the workflow's authority model. |
| B10 | Know when to stop. If agents cause chaos, stop and revert to the last good checkpoint or commit. | Stop conditions and checkpoints in `procedures/execute.md`. POLICY: don't blindly replay side effects after a crash. | Probably covered. Check that a "revert to last good checkpoint" instruction exists. |

---

## IDEA-05 — Choose the development technique per task (TDD / BDD / DDD / prototyping / contract testing / evals)

**Status:** Partly implemented, released in `v1.1.0` — task-appropriate testing examples, practical bug repair guidance and conditional product-AI evaluations. No mandatory technique field, standalone guide or skill adoption. Implemented in T-0003.

**Source:** the owner's document, kept verbatim with its references at [`sources/technique-selection.md`](sources/technique-selection.md). Read that file in full. What follows is only a summary and a first map against the workflow.

**Owner's question (paraphrased):** The owner is unsure about three things. Should the workflow adopt a development technique such as TDD, BDD or DDD? Is it better off without one? Or has it effectively adopted one already? The owner wants the evaluator to decide what would genuinely improve the agent flow.

**The document's core argument:**
- Keep **one consistent discipline** for every task: understand → define success → make a small change → verify → review and record.
- Choose the **technique per task, even per feature**, based on uncertainty, the consequences of failure, complexity and expected lifetime. Project size is not the deciding factor.
- The techniques aren't rivals because each answers a different question:
  - Prototyping / user-centred design: is this the right experience?
  - BDD / specifications: what should the software do?
  - DDD: how are the concepts and boundaries organised?
  - TDD: how do we build it in small, checked steps?
  - Contract / integration testing: do the components agree?
  - Evaluations: does an AI feature inside the product behave well enough?
- The biggest risk: when an AI writes the spec, the code and the tests, all three can agree with one another and still miss the real requirement. Expected results must be checked **against the business requirement**. Agreement between two agents is not proof.

**First map against the current workflow (unverified; check it):**

| Document's point | What seems to exist already | Possible gap |
|---|---|---|
| Consistent discipline: understand → define success → small change → verify → review/record | Readiness → execute → review → accept (`procedures/`). Checkpoints and durable records. | Looks covered, and in stricter form. |
| Choose the technique by uncertainty, consequences, complexity and lifetime | Readiness sets the *level of detail* required. `templates/design.md` is mandatory for money, stock, security boundaries, sync, shared contracts and irreversible data operations. `procedures/execute.md` lists check *types* to "choose applicable". | **No explicit technique-selection step.** Nothing asks "which technique suits this task, and why". `templates/task.md` has a free-text "Approach" section. Would a short selection guide plus an `approach`/`technique` field help, or would it only add paperwork? |
| Prototyping when the experience is uncertain; prototype code isn't production | POLICY § Discovery allows "explicitly labelled prototypes". POLICY requires prototypes to be kept distinct from approved requirements. Bounded spikes cover feasibility. | Mostly covered. Is there a clear route from **prototype → approved requirement → production task**? |
| BDD: agree concrete examples with the requirement owner first | Feature readiness requires acceptance examples. Stable acceptance IDs (`AC-…`). Tests map to IDs. Owner acceptance tests scenarios. | Largely covered in substance. Should the Given/When/Then format be recommended for acceptance examples? |
| TDD done properly: red → green → refactor, and "writing tests after" is not TDD | No mention of test-first. Tests are required, but *when* they are written isn't specified. | **Possible gap.** Should test-first be *preferred* for clear rule/calculation logic, with evidence of the observed failing run recorded in the checkpoint? The owner already has a `tdd` skill installed. Check whether it fits the toolbox process. |
| DDD for complex business domains, but only as much as complexity justifies | Design records cover ownership and lifecycle. Feature readiness includes "ownership/lifecycle". | Probably enough. Possibly guidance on when a domain model or glossary is warranted (there's also a `domain-modeling` skill installed). |
| Interfaces first, plus contract and integration tests between components | POLICY: agree shared contracts before work diverges. Approved contracts can satisfy dependencies. Design records are mandatory for shared contracts. | Contracts are covered. Is **contract testing** (such as consumer-driven tests) named as a check type? |
| Bug fixes: reproduce → regression test → scoped fix → verify the original symptom | POLICY and readiness accept a documented failing baseline for a bug fix. Regression tests appear among the check types. | Is there a **lighter bug-fix lane** that doesn't demand full feature-level readiness? Is "reproduce first, then confirm the original symptom is gone" required? |
| High-consequence work: test what must never happen; property-based testing; explicit access-control expectations | Check types cover permissions, data exposure, failure/retry/recovery. Design records are mandatory for security/money/irreversible data work. | Possibly add **negative / "must never happen" examples** to acceptance examples, and name property-based testing as an option. |
| AI features inside the product (chatbots) need evals, not just tests | Not covered. | **Real gap for the owner's projects** (e.g. the Al Nahwi WhatsApp assistant). Should the profile/readiness recognise "the product contains model behaviour" and require an eval set with quality criteria? |
| Report passed / failed / not tested; never turn "not tested" into "probably works" | POLICY: use "not verified" when evidence is unavailable. Limitations in the acceptance package. Unavailable required checks block acceptance. | Looks covered. |
| Don't weaken or remove tests just to get a pass; test changes need a stated, reviewed reason | POLICY § *Rules for test changes*, `procedures/review.md`. Validator coverage comparison. | Covered, and in strong form. |
| Check expected results against the business requirement, not the generated code | Independent review checks "whether tests still represent approved behaviour". Acceptance examples come from approved requirements, not code. | Mostly covered. Check whether tests *authored by the implementer* are compared against owner-approved examples, or only mapped to their IDs. |
| Keep main instructions short, with pointers to detail | `AGENTS.md` is short and points to procedures. | Covered. |
| Adjust the process by defects, rework, review effort and total cost | POLICY § maintenance: judge by recurrence, severity, wasted effort, observed benefit. | Covered. |

**Questions for the evaluator:**
1. Has the workflow already adopted the *substance* of this document without naming the techniques? If so, is naming them (a short technique-selection table in a procedure) worth the extra text?
2. Which gaps are real and worth closing? Candidates: a technique-selection step, test-first preference with evidence, a bug-fix lane, negative examples, evals for AI features, contract testing as a named check.
3. Would any addition make simple tasks heavier? The document itself warns against demanding a long planning phase for small, obvious changes.
4. Could the café example (Bun Alkaif) or the Al Nahwi assistant serve as a representative fixture or pilot for whatever is adopted?

**Expected output:** a verdict per gap (adopt / already covered / reject), and for anything adopted, the smallest change to procedures, templates or fixtures.

---

## IDEA-06 — Setup cost: a plan for new projects and for existing repositories

**Status:** Implemented on 2026-09-25, released in `v1.1.0`. Under IDEA-10: actor tags and a stop rule in `procedures/setup.md`, a setup reading list, an existing-repository lane, and the `bin/wf-adopt` scaffold (directions 2, 3, 5, 6 and 7). In the backlog pass the same night: direction 4 as a default allowance of half a working day of the agent's wall-clock time across the agent steps, recorded per step in `templates/setup.md`, which the owner may change in the setup record; a token limit was not added because no harness reports tokens reliably to the agent (see IDEA-10 on budgets). The owner's "port instead of adopt" proposal is recorded as a per-repository option the owner chooses in the setup record, not a rule: porting simplifies the owner steps only, and it loses GitHub-side history and integrations, so a blanket rule would cost more than it saves. Originally recorded 2026-09-25 from the owner's report of a real adoption attempt that exhausted the agent's usage allowance.

**Owner's words (verbatim):**
> Set up process is taking too long we need to set rules for that in which you cannot actually adopt the agent workflow if you have already a repository in place that repository need to be ported into a new repository that have been built around agent workflow . Because I was trying to set this up but what I got instead is my AI agent has burned all of my usage limit. Which makes it very expensive to actually try to use agent workflow with an existing repository in that in that way that it was actually doing it’s not efficient. It’s burning tokens and it’s something that I would not like to be happen in the next time I actually work on a project if it’s going to be automated like scripted without actually using the power of LLM’s and agents that would be fine or if there is just a minimal set up to be done by the agent that would be fine also but burning my whole tokens is something that I would like to solve in agent workflow we need to actually sit away in which how we are going to use agent workflow with a new project or with an existing already project so we need to have a way or a plan in motion for both scenarios. This is how I am interrupting the situation and the thing that I have noticed

**Restated:** The owner tried to adopt this workflow in a repository that already existed. The agent doing the setup used up the owner's entire usage allowance before setup was done. The owner wants three things. First, a rule about existing repositories; the owner's own proposal is that an existing repository is not adopted in place but ported into a new repository built around the workflow. Second, a setup that costs little or nothing in model usage: either scripted with no model at all, or a small, bounded amount of agent work. Third, a written plan for both starting points, a new project and an existing repository, so the next adoption does not repeat this.

**Evidence available:** the owner's report only. The session that burned the allowance is not recorded in this repository, and the owner confirmed on 2026-09-25 that no per-step usage breakdown exists. Which steps consumed the usage is unknown and will stay unknown; do not ask again. The design gaps below are visible in the procedure text itself and do not depend on that session.

**Known related report:** issue [#2](https://github.com/Cazy00/agent-workflow/issues/2) (routine bookkeeping requiring governing approval) is a different problem, but it is also about procedural cost. Repeated procedural cost is a recognised report category in POLICY § 12.

**Note-taker's first read of the current rules (verify these):**

- **One setup path for two situations.** `POLICY.md` says the workflow applies to "new and existing software projects", but `procedures/setup.md` and the *Adopt* section of `QUICKSTART.md` describe a single sequence. Nothing distinguishes a fresh repository from a codebase with history, existing branch rules, CI and integrations.
- **The setup budget is in days, not usage.** POLICY § 11 and `setup_budget_days: 2` set a two-working-day timebox. `templates/setup.md` records usage after the fact. There is no usage or spending limit for setup and no stop rule during it. By contrast `procedures/execute.md` requires an agent to "check the remaining budget before each assisted call" during milestone work. The one place a project is most likely to spend uncontrolled usage is the one place with no usage limit.
- **The steps are not marked by who performs them.** Of the eleven steps in `procedures/setup.md`, steps 2, 5, 6, 9, 10 and 11 need the owner's accounts, keys, branch protections, a disposable repository or an observer, and several of them must not be done by an agent at all (credentials, protections, trust anchors). Steps 1, 3, 4, 7 and 8 can be done by an agent or a script. The procedure does not say which is which, so an agent reading it as a to-do list will attempt, retry or wait on steps it cannot complete.
- **Step 3 is mechanical but has no script.** Copying `templates/`, `procedures/` and `.agents/skills/workflow/`, writing `docs/workflow/config.json` from `config.default.json` with the adopted tag and full hash, creating the record directories and writing short `AGENTS.md` / `CLAUDE.md` adapters is deterministic. The validator's commands are `records`, `readiness`, `paths`, `ci`, `acceptance`, `lifecycle`, `session`, `report`, `runtime` and `prepare-evidence`; there is no `init` or `adopt`. Today this step is done by hand or by a model.
- **Step 4 invites a full-codebase inspection that the validator does not need.** The step says "classify all paths by effects, including migrations, scripts, dependency/configuration files, infrastructure and generated inputs/outputs" and "unclassified paths block integration". Read literally against an existing repository, that is a walk of the whole tree. But `classifyPaths` in `validator/lib/paths.js` classifies only the paths changed in a candidate, and `wf ci` blocks only when a *changed* path is unclassified. `config.default.json` already covers most source through broad globs. For an existing repository, classification is a config edit of a few stack-specific globs (for example `*.dart`, `*.vue`, `Cargo.toml`, `requirements.txt`, which the defaults do not list), not an inventory. The procedure does not say this.
- **The reading load before any project file is opened is large.** `POLICY.md`, `SCHEMA.md`, `QUICKSTART.md`, `AGENTS.md`, the procedures and the templates total about 14,500 words. The entry chain is `CLAUDE.md` → `AGENTS.md` → `README.md` → `QUICKSTART.md` → `procedures/setup.md` → `identity.md`, `approval-evidence.md`, `operations.md` and the templates. POLICY § 1 *Daily use* already says to load the short guide, the relevant procedure and the current records, and to consult the full policy only when a rule is unclear; setup has no such short reading list, and an agent that follows every pointer loads most of the corpus into one context.
- **Step 1 for an existing codebase is discovery in disguise.** The profile's *Starting position* and *Technical context* sections, filled from an existing repository, are a codebase study. POLICY § 4 allows a project to "begin discovery with an incomplete profile" and blocks only work that needs the missing information; nothing in setup says to use that allowance and leave *Unknowns* filled rather than researching them up front.
- **Steps 8 and 9 are each a session of their own.** Running the fixtures, testing instruction discovery in a fresh Codex session and a fresh Claude Code session with transcripts, and exercising the real approval path in a disposable repository are each separate work. Done inside the same context as steps 1 to 7, they compound the cost.
- **A plausible shape of the failed attempt (hypothesis, not evidence):** the agent loaded the corpus, tried to fill the profile and classify paths by reading the existing codebase, and attempted or retried owner-only steps, all in one long session with no usage stop. No breakdown exists to confirm or rank these causes, so the evaluator should treat each as unverified and close all of them by design rather than pick one.

**On the owner's proposed rule (port into a new repository instead of adopting in place):**

- Porting does simplify the owner-side steps. A repository created fresh under the owner account gets protections, `CODEOWNERS` and worker access from its first revision, with no legacy branch rules, unknown collaborators or old CI secrets, and a clean point at which "normal protections become active" (step 5).
- Porting does not by itself reduce the agent-side cost. The profile, the classification globs, the baseline results and the acceptance definitions describe the same code wherever it lives. If the setup procedure still tells the agent to inventory the codebase, it will do so in the new repository too.
- Porting has costs of its own: issues, pull requests, CI configuration, deploy hooks, webhooks and any external system that points at the old repository. Git history can be carried over, but the GitHub-side history cannot.
- So the note-taker's first read is that "port" is a good *option* inside an existing-repository lane, and possibly the recommended one when the old repository's protections are messy, rather than a prohibition on adopting in place. The owner decides; the evaluator should give evidence either way.

**Candidate directions to evaluate (not decisions):**

1. **Two lanes with the same controls:** a *new project* lane and an *existing repository* lane in `procedures/setup.md` and `QUICKSTART.md`. Both keep every control in POLICY § 11; the difference is sequencing and who does each step. POLICY § 11 already permits narrowing initial scope or extending the budget but forbids silently dropping controls, so the saving must come from automation and ordering, not from fewer safeguards.
2. **Tag each setup step by actor:** *script*, *owner* or *agent*. Add one rule: the agent performs only agent steps, lists the owner steps with what it needs from each, and stops. It does not attempt, retry or wait on an owner step.
3. **A scripted scaffold with no model:** a `wf adopt` command, or a shell script beside `bin/wf`, that performs step 3 deterministically from the workflow checkout: copy the shared files, write `config.json` with the tag and full hash, create the record directories, write the `AGENTS.md` / `CLAUDE.md` adapters, seed empty `acceptance.json` and `acceptance-map.json`. Testable with a fixture; costs nothing in usage.
4. **A setup usage limit and a stop rule:** alongside `setup_budget_days`, a usage or spending limit for setup, recorded through the existing `wf runtime` `usage` event from `procedures/operations.md`, with the same "check before each call, save and stop at the limit" rule that milestone execution already has.
5. **An existing-repository lane that defers discovery:** start from the minimal profile (purpose, scope, starting position, authority, execution) with *Unknowns* filled honestly, adopt the default classification globs plus the stack's few additions, do not inventory the tree, and make the first milestone the observed pilot, small and bounded. The codebase is then learned per task through task scope, which is how the rest of the workflow already works.
6. **A setup reading list:** name the files an agent reads for setup (`procedures/setup.md`, `templates/profile.md`, `templates/setup.md`, `config.default.json`) and state that `POLICY.md` and `SCHEMA.md` are consulted only when a rule is unclear, mirroring POLICY § 1 *Daily use*.
7. **An owner checklist for the owner steps:** a printable or interactive list for steps 2, 5, 6, 9 and 10 so the owner can complete them without an agent in the loop.

**Related:** IDEA-09 S18 (a one-script install and a 30-minute claim, as evidence for direction 3), S10 (a numeric reading budget, for direction 6) and S3 (the owner's verbatim words as a cheap first profile input).

**Questions to answer:**
- The cause of the failed attempt is unknown and cannot be recovered. Which of the gaps above would each have been sufficient on its own to exhaust an allowance, and does the proposed design close every one of them? The next adoption should record usage per step so this question can be answered next time.
- Which steps can be scripted deterministically, and which genuinely need judgement? Where exactly does the agent stop and hand to the owner?
- Is deferring full classification compatible with "unclassified paths block integration"? The validator suggests yes, because the block applies to changed paths; confirm in `validator/lib/paths.js` and `validator/lib/ci.js` and in fixture 08a.
- Does porting into a new repository reduce any of the costed agent steps, or only the owner steps? Which of the owner's projects would benefit from porting and which would lose integrations?
- What is a sensible setup usage limit, in what unit, and how is it measured across tools?
- Would a two-lane setup make the *new project* case heavier? It should not; the new-project lane is the existing procedure with actors marked.

**Constraints that already apply:** a working agent must not weaken its own rules (`AGENTS.md`); controls may not be removed to save cost, only sequenced, scripted or explicitly narrowed with the owner's decision (POLICY § 11); credentials, keys and protections stay with the owner (POLICY § 7, `procedures/identity.md`). Any change goes through `procedures/maintenance.md` as an improvement proposal with independent review, owner approval, a new version and explicit adoption.

**Expected output:** a two-lane setup design, one for a new project and one for an existing repository, with every step tagged script / owner / agent, a setup usage limit with a stop rule, and a decision with reasons on the owner's "port instead of adopt" rule. If a scaffold command is adopted, its specification and a fixture. The smallest change to `procedures/setup.md`, `QUICKSTART.md`, `templates/setup.md`, `templates/profile.md`, `config.default.json` and, only if scripting is adopted, `validator/`.

---

## IDEA-07 — What happens after development: inside the flow, or something else?

**Status:** Implemented, released in `v1.1.0` (2026-09-25 backlog pass): one short procedure, `procedures/operate.md`, covering deployment, incidents with a hotfix lane, routine maintenance as a standing milestone, product feedback as a discovery input, workflow-version upgrades for a project, and an authority-by-stage table with defaults (production, rollback, spending and deletion reserved to the owner). Reuses tasks, milestones and decisions: an incident is a decision of type `fact` plus a task; no new record type. Wired in through the profile template's *Operations* and *Authority* sections, `execute.md` (bug-fix lane points to the hotfix lane), `accept-release.md` and `templates/release.md` (recovery counts as verified only when rollback was exercised once and a backup restored once), the workflow skill table and `AGENTS.md`. Evidence that grounded the deployment section: PrintFlow's D-0063 makes `main` production and deploys by itself, so the case "release authority is the approval of the release pull request" is real. Not done, and why: no runbook or incident template (the release record's *Post-release verification and recovery* and *Remaining issues and support handoff* sections carry the runbook; another template would add reading cost for no new field); the hotfix review window is a default of one working day until the owner sets one; the owner's per-project answers (which activities, which environments the agent may touch) go in the profile, not here. Recorded 2026-09-25; owner direction added the same day: the flow covers the full lifecycle, from preparation through deployment, maintenance, updates and upgrades.

**Owner's words (verbatim):**
> Thought about something else now we are concerned and really invested into the development phase itself. What about the things that usually happens after development should we be including it within the agent flow or it’s going to be something else? This is something we actually need to think about.

**Owner's direction (verbatim, later on 2026-09-25):**
> Note that the agent needs to cover everything from preparation for development, the development itself, the deployment and the maintenance and the updates and upgrades down the way.

**Restated:** The workflow is thorough about building software: readiness, implementation, verification, review, acceptance, release. The owner first asked whether what happens once a product is live belongs in the flow, then answered it: the agent's work spans the whole lifecycle. Five stages are named: preparation for development, development, deployment, maintenance, and updates and upgrades. The evaluator's task is to design that coverage with the same records and authority rules, at the smallest cost, and to say which actions in the later stages stay reserved to the owner. A direction recorded here is the owner's stated intent; it becomes policy through the normal approval route, like every other change.

**Note-taker's first read of the current rules (verify these):**

- **The flow ends at release.** POLICY § 1 states the flow as "Discover → Establish readiness → Authorise a milestone → Implement and verify tasks → Independently review → Verify the assembled milestone → Obtain product acceptance → Release when authorised → Improve from evidence". The last step, § 12, is about improving *the workflow* from reported friction, not about improving *the product* from production evidence.
- **What exists after development today:**
  - The profile's *Operations* section (`templates/profile.md`): monitoring, recovery, migrations, support, device provisioning and updates, or "not applicable with reasons". It is information; nothing consumes it after release.
  - Release readiness (POLICY § 10, `procedures/accept-release.md`, `templates/release.md`): configuration and secrets, permissions, migration and compatibility, monitoring and failure visibility, backup, restore, rollback or forward recovery, deployment and support instructions, device provisioning and updates, deferred information and release authority. The release record also has *Deployment result*, *Post-release verification and recovery* and *Remaining issues and support handoff*.
  - The consequential-changes summary has an *Operations* row (deployment, recovery, provisioning and update changes).
  - The bug-fix lane in `procedures/execute.md`: reproduce, add a regression check, scoped fix, recheck the symptom, with existing readiness, checks and review still applying.
  - Authority limits that would bind any operating activity: "starting another milestone or deploying requires the authority defined in the profile or a subsequent owner instruction" (POLICY § 8); destructive operations outside existing authority are reserved decisions (POLICY § 5); candidate code must not run with owner credentials (POLICY § 9); `wf lifecycle --stage release` validates evidence and "does not deploy".
- **What does not exist:** no procedure for the operating phase. Nothing covers incident handling (who is alerted, what an agent may do in production, who may roll back), routine maintenance (dependency and security updates, key and certificate rotation, backup verification), what to do with monitoring output, how support requests and user feedback enter discovery, cost review, data retention, or decommissioning. There is no record for an incident, a change window or a production observation, and no hotfix lane: the bug-fix lane is correct for quality but has no stated fast path for an outage. The release record's *Remaining issues and support handoff* is a one-time handoff, not an ongoing loop.
- **The feedback record is for the workflow, not the product.** `templates/feedback.md` and the central issue route in `procedures/operations.md` carry *workflow* friction. Product feedback from users has no defined entry point; in practice it would be an issue on the project's own repository, which the readiness procedure can treat as a discovery input, but nothing says so.

**Stage map against the current workflow (note-taker's first read; verify):**

| Stage the owner named | What exists | What is missing |
|---|---|---|
| Preparation for development | Discovery and the three readiness levels (POLICY § 4, `procedures/readiness.md`); project setup (`procedures/setup.md`); profile, milestone, task and decision records. | Nothing structural. The cost problem is IDEA-06. |
| Development | Execution, checkpoints, four verification layers, independent review, acceptance (`procedures/execute.md`, `review.md`, `accept-release.md`). | Nothing structural. |
| Deployment | Release readiness items, release authority, the release record with *Deployment result* and *Post-release verification and recovery* (POLICY § 10, `templates/release.md`); `wf lifecycle --stage release` validates evidence. "Deploying requires the authority defined in the profile or a subsequent owner instruction" (POLICY § 8). | A deployment procedure: environments (test, staging, production), who runs the deploy, whether the agent may deploy to non-production by default, verification after deploy, rollback authority, and how deployment credentials stay out of the agent's reach (POLICY § 9: candidate code must not run with owner credentials). |
| Maintenance | The bug-fix lane in `procedures/execute.md`; profile *Operations* section as information; the consequential-changes *Operations* row. | Incident handling and a hotfix lane with an explicitly authorised order for outages; monitoring review; backup verification; support and user feedback as discovery inputs; a way for a production observation to become a task without a full new milestone. |
| Updates and upgrades | Product feature updates are new milestones (exists). Workflow version upgrades for a project are required to be explicit (`procedures/setup.md` step 3, POLICY § 11: active milestones stay on their adopted version). | Routine dependency, security and platform upgrades as a bounded standing milestone type with limits; a procedure for migrating a project to a newer workflow version. The word "upgrades" covers both product and workflow; the design should treat them separately. |

**Framing that was open before the owner's direction:** the note-taker had listed two options, a development-only workflow with a defined handover at release, or operations as recurring inputs to the same flow. The owner's direction rules out the first. What remains of it is still useful: the release record should hand over enough (runbook, monitoring, support contacts, rollback) that the owner can operate if the agent is unavailable.

**Design constraints the evaluator should keep:** reuse tasks, milestones and decisions rather than adding record types; put the agent's authority per stage in the profile's *Authority* section as delegated or reserved, default reserved for production, spending and destructive operations (POLICY § 5); keep every later-stage change under the same four verification layers, with only the *order* relaxed for an authorised emergency; keep additions to one short procedure or paragraphs in existing ones, in line with the cost concern in IDEA-06.

**Related:** IDEA-09 S12 (a SHIP gate that requires rollback exercised once and a backup restored once; an INCIDENT declaration as the minimal hotfix lane) and S17 (a pivot procedure that re-cuts open work).

**Questions to answer:**
- Which post-development activities does the owner actually face on current projects? For example, for the WhatsApp assistant: hosting, message failures, updating prices and products, monitoring, backups. Ground the design in those, not in a generic operations list.
- Should the agent have any production authority by default? Which actions are always reserved?
- Is a hotfix lane needed, and what review timing is acceptable for an emergency under owner instruction?
- How does product feedback enter discovery? Do issues on the project's repository suffice, or is a record needed?
- What must the release record hand over so the owner can operate without the agent, even though the agent is expected to keep operating?
- Which deployment targets may the agent touch by default, and which need owner instruction each time?
- Is a project's workflow version upgrade a milestone of its own, and who authorises it?
- Given IDEA-06, how is any addition kept small? Prefer a paragraph in `procedures/accept-release.md` or one short `procedures/operate.md` over new record types.

**Expected output:** a lifecycle design covering the five stages, each mapped to the existing procedure that carries it and naming the smallest addition where one is missing; an authority table per stage (delegated / reserved); the smallest change: profile *Operations* and *Authority* fields, a deployment and maintenance procedure or paragraphs in `procedures/accept-release.md` and `execute.md`, a hotfix lane if adopted, and a note on workflow version upgrades for projects. State explicitly which actions stay with the owner and why.

---

## IDEA-08 — A progress view for the owner who oversees rather than implements

**Status:** Implemented, released in `v1.1.0` (2026-09-25 backlog pass), as direction 2: `wf status` in `validator/lib/status.js`, a derived, read-only view printed as Markdown (or JSON with `--json`) that needs no baseline and never checks approval. It shows, in this order: the project's `measure:`; **Waiting on the owner** (open or proposed reserved decisions with what they block, milestones verified and awaiting acceptance or accepted and awaiting release, unchecked owner setup steps); **Next for the agent** (inbox items awaiting triage, unchecked agent setup steps); milestones with their tasks, each open task's readiness previewed as if the baseline were approved so the real blockers show; blocked tasks with resume conditions; open factual questions for the agent; open workflow feedback; record errors. Tests in `validator/test/status.test.js`. Not done, and why: direction 1 (a GitHub Projects board) needs no code and is the owner's to try; direction 3 (a committed `STATUS.md`) is one redirect of the command's output and must be marked derived if committed, so it is not automated; direction 4 (a live dashboard) is not justified before the command has been used; usage and claims stay in the private runtime state and are read with `wf runtime --action status`; current activity is only visible through claims and pull-request handoffs, which the view says plainly. Recorded 2026-09-25. A GitHub form of directions 3 and 4, an issue republished from `wf status` by a GitHub Action, was implemented at the owner's request under IDEA-12 and released in `v1.2.0`.

**Owner's words (verbatim):**
> another idea, i feel we lack a UI for the prograss and the ongoing work, what is done , what is not , the blockings, prerequisites, etc..... like the UI where i (the developer who is working with the agent knows what is going on , etc..... , note that i migh not do any work just manage and overseas everything and coordinate )

**Restated:** The owner wants one place to see the state of the work: what is done, what is not, what is blocked and why, which prerequisites are unmet, and what is happening now. The owner expects to spend most of their time managing, overseeing and coordinating rather than writing code, so the view is for a supervisor, not an implementer. Today that picture has to be assembled by reading records, running validator commands and opening pull requests.

**Note-taker's first read of the current rules (verify these):**

- **The data already exists and is structured; the gap is presentation.** Every fact the owner asks for is in a record with a stable field (`SCHEMA.md`): task `status` (Draft, Ready, Active, Blocked, Done) with `prerequisites`, `decisions`, `deferred_inputs`, `resume_condition` and the `implemented` / `verified` / `accepted` / `released` stages; milestone `status`, `tasks`, `prerequisites`, `limits` and `stop_conditions`; decision `status` (Open, Proposed, Resolved), `owner`, `required_before` and `affects`. `wf readiness` returns Ready, a bounded subset, or the missing prerequisite by name; `wf runtime --action status` returns milestone usage totals, exposure and budget status; `wf report --action status` returns a report's delivery state. All commands print JSON. Nothing renders them together.
- **Part of the picture lives on GitHub, not in the repository.** Since T-0006, session outcomes, checkpoints, handoffs and the "next eligible action" are posted on the task's pull request or issue, and task records are deleted after merge. A view of "what happened last session" and "what is the agent doing now" therefore needs the pull requests and issues as well as the records; the repository alone shows the plan, not the progress. The private runtime directory (`procedures/operations.md`) holds the active claim and usage, outside the repository and readable only on the coordinator's host.
- **The owner's own queue is scattered.** The things only the owner can do are reserved decisions with status Open or Proposed (`decisions/`), acceptance packages awaiting the owner's test (`templates/acceptance.md`), receipts to sign (`procedures/approval-evidence.md`), release authority, and setup steps that need owner accounts (see IDEA-06). Each is discoverable, but no single list says "these items are waiting on you". For an owner who coordinates rather than implements, that list is probably the most valuable view.
- **Any view must be derived, never authoritative.** POLICY § 3 *Optional navigation tools* and the workflow skill say derived information never supplies authority, gates read authoritative records directly, and agents verify consequential claims against the governing source. A status view is exactly such a derived index. Two consequences: it must be read-only, and it must not become the place where approval happens. An agent-written `approved` field is not approval evidence (POLICY § 6), and a button that writes one would not be either.
- **No secrets or private material may leak into it.** If the view is published anywhere (a static page, a GitHub Project), the public data boundary in `procedures/operations.md` applies: no tokens, no transcripts, no private identifiers. Usage figures and claims from the private runtime directory are for the owner's local view only.
- **Tool-neutrality applies.** POLICY § 3 requires records and procedures to work for Codex, Claude Code or another compatible agent. A view built into one harness's UI would not satisfy that; one built from the records and the validator's JSON would.
- **The existing resume prompt is a natural row in the view.** `procedures/execute.md` requires the agent, when stopping, to state the next action and supply a ready-to-use continuation prompt. A view that shows the next eligible action per task, with that prompt, gives the coordinating owner something to act on directly.

**Candidate directions to evaluate (not decisions), cheapest first:**

1. **Use GitHub as the view, no code.** A GitHub Project board over the project's issues and pull requests, with one issue per task and labels for Blocked, Needs owner decision and Ready for acceptance. Handoffs already land there. Costs no implementation and works from a phone. Weakness: the board mirrors records by hand or by a small sync, and drifts unless the sync is part of the handoff step.
2. **A `wf status` command.** A read-only validator command that loads the records at a revision (reusing `loadAll` and `evaluateReadiness`), optionally the private runtime state, and prints one summary: milestones and their tasks by state; blocked tasks with resume conditions; unmet prerequisites; open and proposed decisions grouped by owner and required-before stage; usage against limits; and a "waiting on the owner" list. Text and JSON output. Zero dependencies, testable with the existing fixtures, tool-neutral. It does not read GitHub, so "last session" comes from the handoff link in the record.
3. **A static page generated from the same command.** `wf status --html` writing a single local file the owner opens in a browser, or a Markdown `STATUS.md` regenerated at each handoff. Cheap on top of direction 2. If committed, it is a generated planning artifact and must be marked derived.
4. **A live local dashboard.** A small local web page that re-reads records, runtime state and, through the GitHub CLI, the open pull requests. Most complete and most expensive to build and maintain; probably not justified before directions 1 to 3 are tried.

**Related:** IDEA-09 S1 (the one measure), S2 (a one-page generated status), S4 (an owner input channel and a "waiting on you" list with defaults), S15 (a dashboard generated from files) and S16 (a portfolio line per project).

**Questions to answer:**
- Which questions does the owner need answered at a glance? Candidate list: what is waiting on me; what is the agent doing now; what is blocked and on what; what is done but not yet accepted or released; how much of the limit is used. Confirm and rank these with the owner before designing anything.
- Is the primary view "project status" or "my queue"? For an owner who coordinates, the queue of reserved decisions, acceptances and receipts may matter more than a task board.
- Local or hosted? A local command or file sees the private runtime state; a GitHub board is reachable anywhere but must stay within the public data boundary.
- How is "current activity" shown when the agent's session is not visible to the workflow? The active claim and the latest handoff are the only durable signals; the view should say so rather than imply a live feed.
- Should the view be part of this repository (a validator command, adopted with the workflow) or a separate tool vetted through `templates/toolbox.md`?
- Does anything existing already suffice, such as GitHub Projects, before building? Try direction 1 on one real project and record whether it answered the owner's questions.
- Given IDEA-06, what is the smallest first version, and how is its maintenance cost bounded?

**Constraints that already apply:** derived indexes supply no authority and gates read records directly (POLICY § 3, `.agents/skills/workflow/SKILL.md`); approval comes only from signed receipts, never from a field or a button (POLICY § 6, `procedures/approval-evidence.md`); public views respect the fixed data boundary and never carry tokens, transcripts or private identifiers (`procedures/operations.md`); the solution must work for both supported tools (POLICY § 3).

**Expected output:** the ranked list of questions the view must answer, a choice among the directions with reasons, and for the chosen one the smallest change: a `wf status` specification and fixture if a command is adopted, or a short note in `procedures/execute.md` and `QUICKSTART.md` if a GitHub board is adopted, stating in either case that the view is derived and read-only.

---

## IDEA-09 — Lessons from the "Super System" template (a sibling operating system: one owner, one verifier, builder lanes)

**Status:** Evaluated 2026-09-25 (backlog pass); adopted in part, per-row verdicts below the table. The lighter lane (question 3) is a configuration of this workflow, not a separate package: enforced mode with no key, `bin/wf-adopt`, the owner inbox, `wf status` and delegation to subagents; a second rule set would drift from the first. Recorded 2026-09-25 from the owner's request to compare the template with this workflow and record what could be inherited.

**Owner's words (verbatim):**
> This is a similar idea to agent workflow. So let's inspect it and see what it offers different than what we are offering in agent workflow. And what is the advantages of each agent workflow and this one? What could we actually adopt?
>
> See what can we actually inherit or adopt from this system and add it to the idea backlogs to be studied later on and actually see whether we can implement it or not, whether to implement it or not as well, and whether there are any better alternatives to that idea.

**Source:** `/Users/monta/Downloads/super-system-template` on the owner's machine, inspected in full on 2026-09-25 (58 files, about 135 KB, no Git history). Its licence is unstated ("set by the person who handed it to you"), so the files are not copied here. The note-taker's structural inventory, including what each of its checks actually enforces, is at [`sources/super-system-template.md`](sources/super-system-template.md). Read that file first; what follows only summarises it and maps it against this workflow. If the folder has moved, ask the owner for the original.

**Restated:** The owner has a second, self-contained "operating system" for running projects with AI agents and wants three things. First, an honest comparison: what the template offers that this workflow does not, and what each does better. Second, a list of parts worth inheriting, recorded here so each can be studied, decided (implement or not) and, where adopted, implemented through the normal route. Third, for each candidate part, whether a better alternative exists than copying the template's version.

**What the template is (checked against its files):** a folder that is a Git repository. The owner writes intent verbatim in `MANDATE.md`; an AI *verifier* (a scheduled task, hourly at most) cuts bounded orders for AI *builders* (one `claude -p` lane per order, each in its own worktree), grades every delivery against an executable acceptance command and a sabotage test, rewrites a one-page `STATE.md` every session and appends one line to `LOG.md`. Seven small Python checks run against an export of `main` on every cycle. The owner watches through an Obsidian vault: a generated dashboard, a deterministic knowledge map, and one page (`YOU.md`) where the owner writes typed lines (`PRIORITY:` / `DECISION:` / `NOTE:` / `STOP:` / `ASK:`) that the verifier answers beneath. A `STOP` file halts everything. Its lifecycle is 0 IDEA → 1 CHARTER → 2 RECON → 2b DESIGN → 2c ARCHITECTURE → 3 SPINE → 4 GROW → 5 SHIP, each with one gate. Its core reading is about 5,700 words (constitution, handover, runbook, norms); a session is meant to read about 1,500.

**Shared ancestry (important for the evaluator):** the template is not a rival design. Its section B12 restates, often word for word, rules that already exist in `POLICY.md`: the six session outcomes; the missing-information table and "waiting for an answer never becomes permission to invent one"; a design record for money, stock, security boundaries, sync or irreversible data; two results per pilot (technical and workflow); "acceptance is not a declaration that the code or its security was audited"; decision states Open → Proposed → Resolved with a required-before stage and supersession; acceptance IDs whose removed tags are removed proofs. So most of the template is **the same policy compressed into a lighter shell with an operating loop around it**. The useful comparison is therefore about the shell and the loop, not the rules.

**Advantages of each (note-taker's observation, for the evaluator to confirm):**

*Where the template is ahead:*
- **Cost to read and adopt.** About 5,700 words of core text against this repository's 14,659; seven tiny checks against a validator with fixtures; install by one script in a claimed 30 minutes; no key infrastructure. This is the exact pain in IDEA-06.
- **Owner-facing surfaces.** The one measure, the one-page STATE, a generated dashboard with a "Waiting on you" panel, a portfolio line per project, and an inbound owner page. This is the exact gap in IDEA-08.
- **Product-code guards and a named failure list.** Secrets, duplication ratchet, hallucinated imports, and sixteen AI-coding failure modes mapped to guards. This workflow's validator deliberately checks records and gates, not product code, and its review criteria are generic.
- **A working, if naive, unattended loop.** STOP file, heartbeat with a beacon log, a scheduled verifier with a fixed prompt, builder lanes in worktrees.
- **Pre-development and ship gates stated as single checkable sentences.** Competitor and legal recon before building; a real user tries a prototype unaided before code; data model decided first (precision, identity, tenancy, time); one thin end-to-end path first; SHIP closes only when a real person used it and rollback was exercised once.
- **Anti-bloat rules.** Lessons are capped at 20 notes and must become checks or stay notes; a rule change that cannot name the failure it prevents or the cost it removes is not made.

*Where this workflow is ahead:*
- **Approval is evidence, not text.** Ed25519 receipts on an external trust anchor, an immutable baseline, protected paths, separate owner and worker identities, no self-approval. The template's "only the owner's quoted words resolve a decision" is enforced as "any text after `quote:`", which any agent can write; its trust model is that the owner reads the dashboard.
- **Readiness has real semantics.** Stages, supersession, bounded subsets, freshness closure, task scope enforced on the actual diff, prerequisites from contracts, nine fixtures with tests. The template's gates are regexes over one-line records.
- **Verification is recorded, not reported.** Acceptance tests must run exactly once and pass at the exact candidate revision under a signed receipt; independent review is a separate context with six named areas; integration reruns on the assembled candidate. The template trusts the lane's own "red run and green run".
- **Budgets and recovery.** Cumulative usage with "unknown stays unknown", claims, exposure and survey cadence in a private runtime record; runner requirements (duplicate claims, cleanup, crash recovery) stated and tested before adoption. The template has per-order limits that nothing sums, a pid lock, and "read STATE, else the last LOG line".
- **Tool-neutral, versioned, maintained.** Codex and Claude entry points; a versioned workflow that projects adopt explicitly; a feedback → maintenance → review → release loop with a privacy boundary. The template is Obsidian, `claude -p` and macOS, with no versioning and nothing after SHIP.

**Candidate parts to study (S1 to S18).** Coverage notes were checked against `POLICY.md`, `procedures/`, `templates/` and `validator/` on 2026-09-25; the *gap* and *alternative* columns are the note-taker's first read and are not decisions.

| # | What the template does | What this workflow already has (checked) | Gap and candidate change | Better alternative to weigh |
|---|---|---|---|---|
| S1 | **The one measure.** One sentence per project a non-technical person can check, in the mandate; `STATE.md` shows `Today: NO / YES` with the command and result. Commit counts and percentages are never reported as progress. | Profile *Purpose* asks for "measures of success"; milestones have an outcome and acceptance scenarios; handoffs report evidence, not counts. No single headline measure. | Add one `measure:` line to the profile (project) and optionally to each milestone, phrased for a non-technical reader, with its checking command where one exists. Show it first in any status view (IDEA-08). Cost: one template field. | None needed; the risk is picking a measure that is not checkable. Require the command or the human check to be named. |
| S2 | **`STATE.md`: the current truth on one page**, ≤ 40 lines, rewritten every session, stamped; stale after two days means re-verify first. | No single current-state page. State is spread across task, milestone and decision records, plus PR handoffs; a fresh session recomputes readiness from records. | A derived one-page status, never authoritative (POLICY § 3). Cheapest form: the `wf status` text output proposed in IDEA-08 direction 2, optionally written to a marked-derived `STATUS.md` at handoff (direction 3). The two-day staleness rule maps to `wf readiness` freshness, which is stronger. | Generating it beats an agent rewriting prose each session: the template's STATE is agent-written and can drift from records, which the template itself guards with a dashboard. Prefer generated. |
| S3 | **`MANDATE.md`: the owner's words, verbatim, dated, `M-n` IDs; superseded lines kept; every order must cite a mandate line** (`mandate_check`). | Tasks cite `governing: []` and `acceptance: []`; the validator resolves governing sources and acceptance IDs on the baseline, so "every task cites a governing source" exists in stronger form. There is no record class for the owner's *verbatim* intent; the profile is agent-drafted and owner-approved. | An "owner's words" section in the profile, or a small governing `docs/workflow/mandate.md`, quoted and dated, with stable IDs a milestone can cite. It also lowers setup cost (IDEA-06): the owner's own words cost nothing to obtain, unlike a codebase study. | The backlog already uses verbatim quotes; the decision template's *Accepted answer and evidence* section can hold quotes too. Weigh a new record against a section in the profile. |
| S4 | **`YOU.md`: the owner's inbound page.** Typed lines (`PRIORITY:` / `DECISION:` / `NOTE:` / `STOP:` / `ASK:`); the agent replies beneath each with `→`; a `DECISION:` line is quoted into the register. Every open decision carries `silence: <default>`; work proceeds on defaults when the owner is unavailable, irreversibles wait. | Decisions resolve only through the approval route (signed receipt on the baseline); a free-text author field or an agent-written field proves nothing (POLICY § 6, § 7). Permitted assumptions live in the approved profile. The decision template has no per-decision default. | Two separable ideas. (a) An owner input channel: a single file or the task's PR thread where the owner writes one-line instructions that the next session must read and answer. (b) A `silence:` default per decision. (a) is cheap and fits IDEA-08's "my queue". (b) conflicts with POLICY § 5 unless the default is itself owner-approved; recording a proposed default and its consequence in the decision record is compatible and useful for the owner's queue. | For (a), the task's pull request or issue already exists as the owner-visible thread (T-0006); a comment convention may suffice without a new file. For (b), a decision `type: assumption` with the default stated, resolved by the owner once, is the existing mechanism. |
| S5 | **Anti-vacuity mutation.** Every order names one change to the product that must make the acceptance command go red; the lane shows the red run and the green run. `mutation_required` checks presence. | POLICY § 9: "a tag provides traceability, not proof of test quality"; review inspects helpers and assertions; the task template (T-0003) prefers test-first and asks to "preserve meaningful red/green evidence". Nothing *requires* evidence of a failing run for a new automated acceptance test. | Require, for each new or changed automated acceptance mapping, evidence of one observed failing run (pre-implementation red, or a named mutation) alongside the passing run, in the verification receipt or the review checklist. The template's check is only a word grep; the discipline is the value, not the script. | Real mutation-testing tools (Stryker for JS/TS, mutmut for Python) are heavier and slower; a single hand-chosen mutation per acceptance is the cheap middle. Note the Settled table rejects universal TDD quotas; this is per-acceptance evidence, not a quota. |
| S6 | **An executable acceptance command in every order**, verified to run on the target machine before it is written: "an order with a command that cannot run is a defect of the order". Disagreement between verifier and builder is settled by the command. | Task `verification:` is free text; the profile lists verification commands; readiness requires `verification` non-empty but not runnable. | Readiness rule: a task's verification names at least one command that was run once on the baseline (its result recorded in `baseline_result`). The field exists; the rule does not. | None; this is a sharpening of an existing field. |
| S7 | **Lessons become checks or stay notes; notes capped at 20; the 21st retires one.** A constitution change "that cannot say which failure it prevents or which cost it removes is not made". | POLICY § 12 judges changes by recurrence, severity, wasted effort and observed benefit; fixtures are the "checks"; maintenance is bounded (three reports or thirty minutes). No cap on instruction growth, and IDEA-06 records the corpus at 14,659 words. | Add to `procedures/maintenance.md`: every adopted instruction change states the failure it prevents or the cost it removes, and names the fixture or check that would catch recurrence, or is recorded as a note. Consider a word budget for the entry chain (`AGENTS.md` + skill + one procedure) enforced by a repository-hygiene test. | A word-count test is mechanical and cheap; a "cap of 20" is arbitrary. Prefer the budget on what a session must read (see S10) over a cap on notes. |
| S8 | **`CHECKS/EXTERNAL.md`: sixteen named AI-coding failure modes**, each mapped to a check or marked watched; read before every recon; refreshed quarterly; "never both a check and a rule for the same thing". | Review covers scope, correctness, maintainability, security, regression risk and test fidelity (generic). Nothing names concrete AI failure patterns (error masking, hallucinated packages, duplication, boundary drift). | A short failure-mode list in `templates/review.md` or `procedures/review.md` for the independent reviewer, each with where the workflow already guards it. Verify the template's 2026 citations before citing them; treat the list as reference. | Keep it to items the reviewer can actually check in a diff; do not import the whole table. |
| S9 | **Product-code checks: `secret_scan` (strong patterns block, weak reported), `duplicate_block` (8-line window ratchet against a baseline), `import_exists` (every Python import resolves).** Run against an export of `main`. | POLICY § 9 lists secret handling, dependency and security checks as check *types* the project chooses; the validator checks records and gates, deliberately not product code. Nothing supplies the checks. | Offer an optional, vetted starter pack for adopting projects through `templates/toolbox.md`, not inside the validator. The Settled table already defers stack-specific bundles "until a concrete task needs them"; the first adopting project is that task. | Mature equivalents exist and are stack-agnostic: gitleaks or trufflehog for secrets; jscpd for duplication; `npm ls` / `pip check` / lockfile audits for phantom dependencies. Vet those first; the template's scripts are Python- and JS-only. |
| S10 | **Reading budget:** three pages (about 1,500 words) per session; "read nothing else unless a page points you to it"; archive read by search only; the map and dashboard exist "so nobody re-reads it". | POLICY § 1 *Daily use* names four things to load; IDEA-06 shows the setup chain loads most of the corpus anyway. No numeric budget, and the skill says "select only the procedure needed". | State the per-session reading list with an approximate word budget in `AGENTS.md` and the skill, and give setup its own short list (IDEA-06 direction 6). Add the "read nothing else unless a page points you to it" sentence. | Measure first: the corpus is already split into short procedures; the problem may be pointer-following, not page length. |
| S11 | **Pre-development gates:** RECON (competitors, law, existing code checked; "what is ours" stated); DESIGN (a real user tries a prototype unaided before code); ARCHITECTURE (data model first: precision, identity, tenancy, time; threats; performance budget; hostile review until the blocker list is empty); SPINE (one thin path end to end before growing). | Discovery and three readiness levels (POLICY § 4); explicitly labelled prototypes allowed; design record mandatory for consequential changes; milestone is "one coherent journey". No competitor or legal check, no real-user prototype gate, no "data model first" order, no "thin spine first" milestone shape. | Candidates, each small: a discovery output item "what already exists elsewhere, and the legal constraints"; a readiness question "has a real user tried it unaided?" for new experiences (IDEA-05 prototyping); four data-model questions in `templates/design.md`; a recommendation that the first milestone of a new product be a thin end-to-end path. | These are product-methodology choices, not workflow controls. The owner decides whether they belong in a general workflow or in each project's profile. IDEA-05 already covers when to prototype. |
| S12 | **SHIP gate and operating rules:** SHIP closes only when "a real person used it AND rollback was exercised once"; production breaks → "declare INCIDENT in STATE, freeze features, fix, lesson becomes check, resume"; NORMS: a backup is proven by restoring it; expand → migrate → contract for schema changes; flags and staged rollout. | Release readiness lists rollback and recovery as items with evidence "or justified not applicable" (`templates/release.md`); nothing requires the rollback to have been exercised. IDEA-07 records that there is no incident procedure or hotfix lane. | Feed IDEA-07 directly: make "rollback exercised once on the target environment" and "backup restored once" release-readiness evidence where applicable; adopt the INCIDENT declaration as the minimal hotfix lane (freeze, fix under the bug-fix lane, regression check, resume). | The template also stops at SHIP; it does not answer maintenance, updates or workflow upgrades. Use it for the two gates only. |
| S13 | **`STOP` file, heartbeat loop, beacon log.** One file halts builders, loop and verifier; "proof of pause is silence in the log for one full cycle, not 'I told it to stop'"; resume order is fixed. | Stop conditions are stated (POLICY § 8) and budgets are checked before each call, but there is no mechanical kill switch and no runner. | Relevant only when a runner is adopted. The principle "a pause is proven by observed silence" belongs in the runner fixtures (POLICY § 11: "the runner stops at the correct boundary"). | None until then; note it in the runner requirements so it is not rediscovered. |
| S14 | **A scheduled verifier with a fixed prompt, and builder lanes as `claude -p` in worktrees**; builders never merge; the verifier grades and merges; one beat per hour at most. | POLICY § 8 defines runner requirements (authorised milestone and identity, duplicate-claim prevention, state inspection, structured outcomes, cumulative budgets, bounded calls, cleanup, evidence) and requires fixtures plus a pilot before unattended use. The template's loop meets none of those mechanically. | A candidate *shape* for the eventual runner: a scheduled coordinator session with a fixed prompt, and per-task worker sessions in worktrees. It would still need claims, budgets and recovery from `procedures/operations.md`. The fixed prompt (`VERIFIER-BEAT-PROMPT.md`) is a good pattern for the resume prompt already required in `execute.md`. | Claude Code scheduled tasks or a cron job around `wf runtime` claims is the same shape with the workflow's accounting. Do not adopt the loop as is. |
| S15 | **Dashboard generated from files** (portfolio, measure, checks, waiting-on-you, recent sessions, trust panel); **a deterministic, token-free knowledge map** (`graph_map.py`) with an owner-editable vocabulary; verifier commit-share cap of 30%. | IDEA-08 covers the dashboard; POLICY § 3 defers Graphify "until observed navigation problems justify a trial" and asks to evaluate "the simplest suitable option" first. Roles are split differently here (a coordinator may implement), so a commit-share cap does not transfer. | Dashboard: see IDEA-08 direction 2 (`wf status`). Map: record `graph_map.py`'s approach (structural edges only, nothing inferred, archive excluded, every exclusion listed) as the simplest option to trial *if* navigation problems appear; it stays deferred. Share cap: do not adopt. | Obsidian is one viewer; Markdown output is enough. |
| S16 | **`PORTFOLIO.md`: one line per project** (state, priority, next gate, last verified), plus parked and closed tables; "a builder serves one project at a time; shared resources go to the higher priority". | Each adoption is one project; the central repository collects feedback across projects but has no portfolio view. The owner runs several projects. | A portfolio page in the owner's own space (not in any project's governing records), generated from each project's `wf status` if that command is adopted. Part of IDEA-08's "what is waiting on me" across projects. | A GitHub Projects board across repositories (IDEA-08 direction 1) gives this without code. |
| S17 | **PIVOT is a decision, not a drift:** new mandate line, old lines superseded, one PIVOT log line, every open order re-cut or refused, unserved work closed. | Supersession invalidates stale readiness (fixture 04b); scope changes are reserved decisions. No named procedure says "re-assess every open task after a direction change". | One paragraph in `procedures/readiness.md`: after a superseding decision on scope or direction, list every Ready/Active task, rerun readiness, and close tasks that no longer serve an approved outcome. | Freshness already stales affected tasks mechanically; the paragraph adds the human step of closing, not just blocking. |
| S18 | **Install by one script; "ten steps, about 30 minutes".** `INSTALL.sh` initialises the repo and runs the checks; the handover is one 1,800-word page. | `procedures/setup.md` is eleven steps over two days with trust anchor, identity and approval-path tests; IDEA-06 proposes a scripted scaffold and actor tags. | Evidence for IDEA-06 direction 3: the deterministic parts (copy files, write config, seed records, run fixtures) can be one script. The template's speed comes from having no trust infrastructure, so its number is not a target for this workflow. | Keep the controls; script the mechanics; give the owner a printed checklist for the owner-only steps (IDEA-06 direction 7). |

**Verdicts (2026-09-25 backlog pass; the adopted items were released in `v1.1.0`):**
- S1 adopted: `measure:` in the profile and milestone templates, shown first by `wf status`.
- S2 adopted as `wf status`; no committed state page; staleness is already `wf readiness` freshness.
- S3 adopted in part: an *Owner's words* section in the profile and the inbox triage rule quote the owner verbatim with dates; no separate mandate record, because decisions and acceptance IDs already give citable IDs.
- S4 adopted in part: the inbox is the inbound page. `silence:` defaults rejected: POLICY § 5 says waiting is never approval, and reversible defaults already exist as permitted assumptions in the approved profile.
- S5 adopted as an instruction in `execute.md` and the reviewer subagent: a failing run before and a passing run after each new or changed automated acceptance mapping, posted on the pull request. Not mechanical; that would need receipts.
- S6 adopted as an instruction in `readiness.md`: `verification` names commands that run; `baseline_result` is their run. The validator does not execute commands.
- S7 adopted in `maintenance.md`: every instruction change names the failure it prevents or the cost it removes and the check that catches recurrence; corpus word count recorded before and after. No numeric cap on notes.
- S8 adopted: a failure-pattern list in `review.md` and the reviewer subagent.
- S9 rejected for the validator: stack-specific product-code checks go through the toolbox when a project needs them (see the settled table).
- S10 adopted: a per-session reading list with a word figure in the workflow skill; the setup reading list came with IDEA-06.
- S11 adopted in part: one sentence in `readiness.md` (a thin end-to-end first milestone; data-model decisions before data-bearing features). Competitor and legal checks and a real-user prototype gate are product method, left to the owner per project.
- S12 adopted: recovery counts as verified only when rollback was exercised once and a backup restored once (`templates/release.md`, `accept-release.md`); the incident lane is in `operate.md`.
- S13 adopted as one clause in the runner requirements: a pause is proven by observed silence.
- S14 deferred to runner adoption.
- S15 the dashboard is `wf status`; the knowledge map stays deferred (POLICY § 3).
- S16 deferred: a portfolio lives in the owner's own space; `wf status --json` per project is its input.
- S17 adopted: the pivot paragraph in `readiness.md`.
- S18 adopted as `bin/wf-adopt` (IDEA-06).

**Questions for the evaluator (answered above where the verdicts say so):**
1. Which of S1 to S18 are already covered in substance and only need a sentence, and which are real gaps? The note-taker's first cut: real gaps are S1, S2 (as a generated view), S5, S6, S8, S12, S17; cheap clarifications are S3, S4(a), S7, S10; product-methodology choices for the owner are S11; deferred to runner adoption are S13, S14; deferred with IDEA-08 are S15, S16; S9 goes to the toolbox route; S18 is evidence for IDEA-06.
2. Does anything in the template *weaken* a control if adopted literally? Candidates: `silence:` defaults (S4b) against POLICY § 5; an agent-rewritten STATE page as a source of truth (S2) against POLICY § 3; a quoted line as approval (S3, S4) against POLICY § 6 and § 7. Adopt the surface, keep the evidence rule.
3. Should the workflow offer a **lighter lane** for solo, low-consequence projects, closer to the template's weight, while keeping the same records and the same approval rule? This is the recurring theme of IDEA-06, IDEA-08 and this idea; the owner should decide whether "lite" is a configuration of this workflow or a separate package.
4. For S9 and S8, verify the template's research citations and the tools proposed as alternatives before writing anything that cites them.
5. Which parts would the owner's current projects use first? The WhatsApp assistant would benefit from S1, S5, S12; a new product from S11.

**Constraints that already apply:** derived views carry no authority and gates read records directly (POLICY § 3); approval is a signed receipt, never a field, a quote or a button (POLICY § 6, § 7, `procedures/approval-evidence.md`); external scripts and skills go through inspection, pinning, representative testing and a toolbox record before adoption (POLICY § 3, `templates/toolbox.md`); a working agent must not weaken its own rules (`AGENTS.md`); any adopted change goes through `procedures/maintenance.md` as an improvement proposal with independent review, owner approval, a new version and explicit project adoption; and the cost concern in IDEA-06 applies to every addition.

**Expected output:** a verdict per row (adopt / adopt in part / already covered / reject / owner decision / deferred to IDEA-06, -07, -08 or runner adoption), with evidence; for each adopted row the smallest change (a template field, a sentence in a procedure, a review-checklist item, a toolbox entry, a fixture) and a statement that existing safeguards still hold; and an explicit recommendation on question 3, the lighter lane.

---

## IDEA-10 — Owner feedback on the A-to-Z walkthrough: verification without the owner, tests, the signing key, budgets, an inbox, a scripted setup, mid-project changes

**Status:** Implemented, released in `v1.1.0` (2026-09-25, branch `claude/ideas-adoption-and-post-development`, pull request #6). The owner decided the approval model the same evening (see *Owner decisions*); two small points stay open with a default in use.

**Owner's words (verbatim):**
> you are saying that there are: Four layers of checking. The agent tests its own work. A second agent in a separate session reviews it without seeing the first agent's chat. The combined result is re-tested. Then you try the product yourself.
> are those need to be run manually by the developer ? cuz it should not its time consuming this need to happen autonomously the developer will need to only approve the final work of a milestone or presents the edits. the developer will not check the code. he is like a Software engineer that oversees the outcomes, and see of the goals are meet or not. the classic hierarchy we know in softwere development, this mens that out agent-workflow need to utilize the harness that its running on claude code, codex, cursor , etc and can utilize its full potentials like subagents and the ability give orders to span a subagent for a specific task, claude code, codex and smart in this they can span agents if we just plainly tell them to do so. […] i can see some part of it already in number 3 (Milestone authorisation) also in number 4 (Implementation) there is some parts, maybe also can be improved according to what i have said.
> also make sure that there needs to be meaningful tests for each thing we are developing, test are important and its saves time and it will help us catch issues when we change the code and update it, you know where this is going....
> this feels overkill, dont you think ? why we need a cryptographic key ?: No self-approval. […] Approval comes only from a receipt that you sign with a cryptographic key.
> how its going ti do this ? is it relable and acctuly works or it will fail and give Wrong data and reading leading to wrong decisions. ?: Budgets and stop rules. […]
> regarding the Setup, are there any folder structure that will be followed and created ? or it depends ? what do you see ? is there need to be a inbox where i can dumb all data and you will take and organise them ? or how ? will it be batter like the way i says it ?
> i feel that we need to stream line the Setup and have at least a check list and guide for it, we can do the setup in sh setup file ? what do you think ?
> also if i feel that there is something i did not like with the agent-workflow can i overwrite it ? can i improve the agent workflow even if we are half way in production ? i will that will be able to do this, its important.
> how can we fix some of the areas where its likely to break ? fix what ever is fixable now.
> […] i need you address them aiming to make agent-workflow batter, faster, useable, efficient, helpful, we can say also smarter.

**Restated:** Seven points. (1) The four verification layers must run without the owner: the owner authorises a milestone, resolves reserved decisions and accepts the outcome; the agent, using the harness's subagents, does the rest. (2) Every piece of work needs meaningful automated tests. (3) The signing key feels like overkill; why is it needed? (4) Are budgets and stop rules reliable, or will they mislead? (5) What folder structure does setup create, and should there be an inbox where the owner dumps raw input for the agent to organise? (6) Setup needs a checklist and a script. (7) The owner must be able to change the workflow mid-project. And: fix whatever is fixable now.

**Evidence found while answering (verified 2026-09-25):**
- `POLICY.md` never requires a human to run verification, review or integration checks; it requires a *separate context* for review (§ 8, § 9). Claude Code, Codex (subagents generally available since March 2026) and Cursor (since 2.4) each provide subagents that start with an empty context and see only their prompt. The procedures did not say to use them.
- The owner's involvement in layers 1 to 3 today is not running them but **signing their receipts**: `procedures/approval-evidence.md` has the owner-controlled collector review raw evidence and sign verification, review and integration receipts for every candidate.
- **PrintFlow (`~/Dev/PrintFlow`) runs workflow v1.0.0 with no key.** Adopted 2026-09-16 in one day (commits `a8918a7` to `9880ccd`); `approval_mechanism: github-rulesets-codeowners`, `approval_label: enforced`, worker `MontaDev-sys`, ruleset `protect-main` active; 35 tasks and 63 decisions by 2026-09-25. v1.0.0's adoption was five steps, and the installation lives in an ignored `.cache/agent-workflow/` checkout driven by `scripts/wf`.
- The unreleased candidate cannot be adopted by PrintFlow as it stands: `validator/lib/readiness.js` (line 47) fails every readiness check without a signed baseline receipt, and `SCHEMA.md` says the candidate "does not elevate an enforced label into approval evidence". POLICY § 7 permits the protected-branch inference "when the verified protection configuration makes that inference valid"; the candidate implemented only the manual alternative and made it mandatory. `v1.0.0..HEAD`: 23 commits, 201 files, +4,389 lines.
- Budgets: `wf runtime` is a ledger the agent writes, not a meter. Time, task count and retries are always measurable; tokens and spend are only as good as the harness's own counter; unknown stays unknown by design; nothing is stopped mechanically.
- Setup creates a fixed structure (`docs/workflow/{profile.md,config.json,acceptance.json,milestones,tasks,decisions,feedback/inbox}`, `tests/acceptance-map.json`, `scripts/wf`, `AGENTS.md`, `CLAUDE.md`, `.cache/agent-workflow/`); no step was scripted and there was no owner inbox.

**Implemented locally (no owner decision needed, no control weakened; unreleased until reviewed, approved, versioned and adopted):**
1. `procedures/execute.md`: delegation paragraph (subagents run review, integration reruns and bounded exploration; delegates inherit the task's authority, not more; the owner is involved only at reserved decisions, acceptance and reserved approvals); automated tests by default for production-affecting tasks, with a recorded reason and manual evidence method when impractical, checked at review.
2. `procedures/review.md`: how the separate review context is launched as a subagent and what its prompt contains; the owner does not run reviews.
3. `procedures/setup.md`: a setup reading list; every step tagged script / agent / owner; the agent performs only its steps, writes the owner checklist and stops; an existing-repository lane (no codebase inventory, classification as a config edit, unknowns left honest, first milestone as pilot, porting as an owner option); a stop rule with per-step usage recording. (IDEA-06 directions 2, 5, 6, 7.)
4. `bin/wf-adopt` with `validator/test/adopt.test.js`: a deterministic, model-free scaffold that reads every shared file from the pinned revision, writes config with the full hash, seeds records, installs `scripts/wf` and the adapters, fetches the installation into `.cache/agent-workflow/`, never overwrites, refuses a second adoption, and writes the owner checklist into `docs/workflow/setup.md`. (IDEA-06 direction 3; IDEA-09 S18.)
5. `docs/workflow/inbox/` as the owner's drop folder: triage rule in `procedures/readiness.md` (raw, no authority, quoted with date into the right record, moved to `done/`), notes in `SCHEMA.md` and `QUICKSTART.md`. (IDEA-09 S3 and S4a, in the form that keeps the approval rule.)
6. `procedures/maintenance.md`: "Changing the workflow during a project", stating what already holds: explicit, versioned, adopted by the project; active milestones keep their version unless the owner decides otherwise; weakening a control is the owner's decision alone.
7. `templates/milestone.md`, `templates/profile.md`, `procedures/operations.md`: limits in measurable units first; usage recorded from the harness's own counter or null; the delegation feature recorded in the profile.

**Owner decisions (2026-09-25, in conversation, verbatim):**
> Okay, regarding the things that I need to make a decision for, regarding the approval model, let's go with option A. Regarding the other questions, which tool you will use most? So sub-agent feature is the one that exercised in the first pilot. I will be using cloud code for development , i did not get this : "and do you want the scaffold to also create the GitHub repository and branch rules for you when you run it, or stay free of any GitHub writes?"
>
> Just for your information, I said that I would be using mostly cloud code. Okay. I would be also using codecs and cursor as well, but maybe not as frequent. Well, this is going to affect the process if I'm going to use codex or cursor later on ?

Restated: approval model **A**; Claude Code is the main tool, Codex and Cursor occasionally; the GitHub-writes question was not understood and stays at its default (the scaffold writes nothing to GitHub) until the owner asks otherwise.

Implemented for A the same evening: `createEnforcedTrust` in `validator/lib/trust.js`; the CLI builds it when the baseline's config and profile both record `enforced` and the baseline is an immutable Git revision; `lifecycle.js`, `acceptance.js` and `ci.js` list receipt-dependent evidence as `unverified` instead of failing in that mode, run the full receipt gate instead whenever the trust options are supplied, and still refuse `--changed`; a candidate cannot switch modes because config and profile are read from the baseline. Tests: `validator/test/enforced.test.js`. Documents: `SCHEMA.md`, `procedures/approval-evidence.md` (new *Enforced mode* section), `procedures/setup.md` step 6, `QUICKSTART.md`, `README.md`. `bin/wf-adopt` always scaffolds `manual`; the switch to `enforced` is the code-owner-reviewed pull request after steps 5 and 9 (an `--approval` option existed for a few hours and was removed on the independent review's blocking finding: it let the label be written before anything was verified). Enforced mode accepts the baseline as v1.0.0 did; its other checks (lifecycle, acceptance execution, freshness) are stricter than v1.0.0 and list what a receipt would have proven. For Claude Code: `templates/claude/agents/independent-reviewer.md`, installed by `wf-adopt` at `.claude/agents/independent-reviewer.md`, is the separate review context; Codex and Cursor register the same prompt when first used (setup step 8 tests each tool anyway, so using them later changes no record and no procedure).

**Independent review (2026-09-25, separate-context Claude Code subagent per `procedures/review.md`, posted on the pull request):** result *changes required*: one blocking finding (the scaffold's `--approval enforced`) and nine lesser ones (a mid-run failure on a revision without the templates, a contradiction in `SCHEMA.md`, `wf session` accepting verified-complete without linked evidence in enforced mode, an understated corpus figure, inbox counting only Markdown, `wf status` failing on a label mismatch, two undocumented enforced-mode consequences, two dropped phrases in `setup.md`, missing blocking-case tests). All ten were fixed in the same pass before the pull request was opened; the review text is on the pull request.

**Decided (kept for the record):**
- **Approval model.** (A) Restore `enforced` as a supported approval mechanism in the candidate: when the setup record shows verified branch protection, `CODEOWNERS` review and the approval-path test (steps 5 and 9), readiness accepts the fetched authoritative branch as the approved baseline, as v1.0.0 and PrintFlow do; receipts remain the `manual` alternative for repositories that cannot be protected. (B) Keep receipts mandatory but limit the owner's signing to owner decisions (baseline, governing-change, workflow-change, acceptance, release), with verification, review and integration evidence posted on the pull request and attested by CI rather than signed by the owner. (C) Keep the candidate as it is. Recommendation was A; the owner chose A. Manual mode is unchanged for now; B's split remains an option for it.
- **Setup usage limit** (IDEA-06 direction 4): unit and value. Proposed: half a working day of wall-clock time for the agent steps, one session per step group, recorded in `templates/setup.md`; token limits only where the harness shows them.
- **Inbox location:** `docs/workflow/inbox/` (chosen; planning class; ignored by the loader) or `docs/inbox/`.

**Corpus size (per the S7 rule):** measured with `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`: 14,724 words at `58ae362`, before this session, and 18,102 after the review fixes, a growth of 3,378 words (23%). An earlier "before" figure of 15,933 was measured after the first edits and was wrong; the independent review caught it. The growth is `procedures/operate.md`, the restructured `setup.md`, the enforced-mode text in `SCHEMA.md` and `approval-evidence.md`, and the delegation, tests, inbox and status paragraphs. A session still loads only the guide, one procedure and the current records (about three thousand words); setup loads its own short list.

**Still open, default in use:** the inbox location (`docs/workflow/inbox/`). The setup allowance default (half a working day of the agent's time) is now written into `procedures/setup.md` and `templates/setup.md` under IDEA-06. **Answered:** the pilot exercises Claude Code's subagents. **Deferred:** whether `wf-adopt` may also create the repository and ruleset through `gh` when the owner runs it; today it writes nothing to GitHub.

**Constraints that already apply:** as IDEA-06 and IDEA-09.

---

## IDEA-11 — Quality enforced across a growing codebase, with the owner overseeing rather than implementing

**Status:** Partly implemented, released in `v1.2.0` (2026-09-25, pull requests #8 and #9), after a consultation with Fable (below). Implemented: a readiness gate on an empty `required_checks` list; the project's checks as required status checks with branches up to date; recorded conventions and component boundaries, with recurring review findings turned into checks; tested invariants; owner-facing and per-layer fields in the review, acceptance and pilot templates. Needs owner decision: a milestone-branch delivery arrangement. Four items the *Settled* table already decided were touched but not re-opened. Not now: an unattended runner, parallel coordinators, stack-specific checkers.

**Owner's words (verbatim):**
> is this repository going to give us better results in ai software developments for example is it going to actually enforce the pro the proper way of actually coding or building softwares across the whole project [...] if we are building something that then this thing that we are building comes as something as big as erpnext or udo [...] will it be able to actually maintain this size of applications
> [...] i am like a software engineer and i am orchestrating a building of a software lucky me i don't have people actual people i do have ai agents [...] me the human needs to be in the loop to actually check the output [...] ai still lacks one area which is user experience and user design we have solved this or we have overcome this by actually designing the prototype before we actually start coding [...] i am not doing the engineering work i am just an observer the engineering work this engineering work is being enforced maybe by this thing we have built which is agent workflow [...] is this vision that i am spilling out right now happens to be true or it's have it have some gaps in some of the areas
> what will you improve ?
> Okay, those sounds good. First, I would like you to consult Fable on this. [...] If you think that he's making a wrong decision, you can converse back to him and actually tell him your view as well. You can converse and communicate with each other in order to reach to the best agreement and implement it right away.

**Restated:** Does the workflow enforce good engineering across a codebase that grows to ERP size, and can the owner oversee while agents implement, test and review? After the assessment below, the owner approved four proposed changes and asked that they first be checked with Fable, disagreements argued out, and the agreed result implemented.

**Evidence found (verified 2026-09-25):**
- The validator checks records, gates and traceability, never code: `validator/lib/` has no lint, architecture, complexity or duplication logic. POLICY § 9 lists type, build, lint and component-boundary checks among the applicable checks; nothing required a project to wire any of them.
- `validator/lib/readiness.js` never read the profile's `required_checks`, so a project could be Ready with an empty list; named checks were enforced only through a signed verification receipt (`lifecycle.js`, manual mode). Setup step 7 already asked for required checks; nothing enforced it.
- In `enforced` mode `wf ci` lists test execution, the independent review and the integration rerun as `unverified` for the owner's code-owner review (`lifecycle.js`, `acceptance.js`, `approval-evidence.md`). `approval-evidence.md`'s account of setup steps 5 and 9 required the `wf ci` check, but neither it nor setup step 5 required the project's own checks or up-to-date branches, so the integration rerun on the assembled candidate (POLICY § 9, layer 3) had no mechanical form in that mode.
- Nothing recorded component boundaries or conventions; the profile's *Technical context* asked only for "confirmed architecture". Duplicated logic was already a listed review finding; boundary and convention breaks were not.
- `templates/design.md` had an invariants heading, but nothing tied an invariant to an acceptance ID, so the coverage gate could never fire for one.
- Nothing recorded which layer caught which defect, although maintenance judges changes by recurrence and POLICY § 3 waits for observed navigation problems before trying navigation tools.
- Per-task owner review follows from task pull requests targeting the protected `main`, not from a POLICY rule: § 7 requires owner review on the authoritative branch. But `wf ci` evaluates one task per run (`validator/cli.js`, `validator/lib/ci.js`), so a milestone pull request carrying several tasks' production changes fails it today.

**Consultation (2026-09-25):** Fable, in a separate read-only context given the owner's question, the proposed plan and the repository, fact-checked the assessment and gave a verdict per change. Adopted from it: keep POLICY's "applicable" wording for required checks; make every required check a required status check and require up-to-date branches (the gap the plan had missed); record boundaries and conventions in the profile or, when long, in a governing document under `docs/specs/`; add only the boundary and convention half to the review patterns, since duplication was already there; list invariant IDs in the milestone and the task, and say that the gate proves a mapped test exists, not that it ran; give the owner plain-language consequences and evidence rather than diff hunks; and turn recurring finding categories into required checks. One disagreement: Fable first called the milestone-branch arrangement a zero-word profile choice; the coordinator showed that `wf ci` is single-task, so the milestone pull request would fail, and that task records are removed at merge. Fable conceded, and the item moved to *Needs owner decision*. The coordinator split setup step 7 so "first milestone only" no longer reads as applying to required checks, and adopted Fable's clause tying the ruleset update to the owner's approval of the profile change. The optional change naming required checks in `lifecycle.js`'s `unverified` line was skipped.

**Independent review (2026-09-25, separate-context Claude Code subagent per `procedures/review.md`, posted on the pull request):** *approve with notes*. One should-fix finding: the owner could tick setup step 5 before the required checks existed, since agent step 7 defines them; step 5 and the scaffold checklist now say the owner adds each entry when approving the profile change that defines it. Seven notes: the settled items were listed as open decisions (re-headed below); an overstated claim about the old step 5 (corrected above); the operating cost of up-to-date branches (recorded in item 2, and step 9 now observes it); `QUICKSTART.md`, `templates/setup.md` and `procedures/review.md` not brought along (updated); the step number in the readiness message and the two scaffold assertions sharing one test (both accepted as they are); and the implementer, the consultant and the reviewer all being Claude models, so the review's independence is of context, not of model family, which is the question change 5's records can later answer.

**Implemented locally (no control weakened; unreleased until reviewed, approved, versioned and adopted):**
1. `validator/lib/readiness.js`: project readiness fails while `required_checks` is empty; `SCHEMA.md` says so. Failure prevented: implementation starting with no code gate at all. Recurrence check: `fixtures/10-profile-without-required-checks` (readiness blocked, `ci` fails a production change, a planning-only edit still passes), observed failing without the rule and passing with it.
2. `procedures/setup.md` steps 5 and 7, `procedures/approval-evidence.md`, the `bin/wf-adopt` checklist: `wf ci` and every required check are required status checks, with branches up to date before merging; required checks are the stack's applicable type, build, lint, component-boundary and test commands, named as CI reports them; the owner adds each entry to the ruleset when approving the profile change that defines it. Failure prevented: project checks and the integration rerun resting on the owner reading the pull request. Recurrence check: `validator/test/adopt.test.js` asserts both checklist lines, observed failing without the scaffold change; no mechanical check covers the ruleset itself. Cost added: with up-to-date branches required, every open pull request is updated and rerun after each merge, and stale-review dismissal may then dismiss the owner's approval; setup step 9 now records what actually happens.
3. `templates/profile.md` *Technical context*, `procedures/review.md`, `templates/claude/agents/independent-reviewer.md`: components, allowed dependencies and conventions, each with its enforcing check or `none yet`; the reviewer reads them before the code and treats a boundary or convention break as a finding; a recurring finding category becomes a check in `required_checks` through a normal task, and the owner adds it to the ruleset when approving that profile change. Failure prevented: conventions drifting as the codebase grows, and review catching the same pattern indefinitely. Recurrence check: none mechanical (prose); the ratchet is what turns a recurrence into a check.
4. `templates/design.md`: each invariant gets an automated acceptance ID listed in the milestone and the task, and a test that tries to break it (property-based where the stack supports it), or a recorded reason none is possible. Failure prevented: plausible but wrong money, stock or tenancy logic passing review untested. Recurrence check: the existing coverage gate (`acceptance.js`: missing automated coverage, removed required mapping); in enforced mode it proves a mapped test exists, and the failing and passing runs are on the pull request.
5. `templates/review.md`, `templates/acceptance.md`, `templates/pilot.md`: tool and model for implementer and reviewer; a fixed finding category; a *For the owner* section (plain-language consequences, the evidence the approval relies on, what stays unverified); defects by the layer that caught them. Failures prevented: rubber-stamp approvals, and workflow changes made without recurrence evidence. Recurrence check: none mechanical (prose templates); they supply the data maintenance needs, including whether same-model review misses things.

**Needs owner decision (nothing implemented):**
- **Milestone-branch delivery:** task pull requests into an authorised milestone branch, with the owner's code-owner review once per milestone pull request into `main`. Not a POLICY § 7 weakening, but a coarser owner check; it needs a milestone-level `wf ci` mode (a validator change), a ruleset on the milestone branch (pull request required, `wf ci` and required checks, no direct worker push) and a rule for when task records are removed. On PrintFlow `main` is production (D-0063), so the milestone pull request would also be the release.

**Already settled, not re-opened** (listed because the assessment touched them): a coverage floor ("do not add: universal TDD/coverage quotas"); mandating a separate test-writing agent (using one for consequential work is already allowed); cross-model review as a rule (close to deferred model routing; the review template now records models); low-risk pull requests merging without owner review ("automatic merging" was passed on, and it weakens a control). Re-opening one needs new evidence, such as the per-layer counts from change 5 showing a class of defect that review keeps missing.

**Not now:** an unattended runner or parallel coordinators (POLICY § 8 and § 11 require a separately approved runner, fixtures and a pilot); stack-specific architecture checkers (they belong in each project, adopted through the toolbox route). The owed v1.1.0 pilot, with the by-layer table filled in, is the most useful next evidence.

**Adoption note:** a project whose profile has `required_checks: []` loses readiness on every task once it changes its pin, until the list is filled through a code-owner-reviewed profile change; `procedures/operate.md` already has the upgrade task migrate the records a new schema needs.

**Corpus size (per the S7 rule):** measured with IDEA-10's formula, `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`: 18,060 before (at `7d05561`) and 18,520 after the review fixes, a growth of 460 words (2.5%). The reviewer subagent file, outside that formula but read in every review (`wc -w templates/claude/agents/independent-reviewer.md`): 485 before, 528 after.

---

## IDEA-12 — The progress view on GitHub, from the tracker already in use

**Status:** Implemented, released in `v1.2.0` (2026-09-25, pull requests #8 and #9). A GitHub Action that `bin/wf-adopt` installs republishes `wf status`, with the open pull requests, to a *Project status* issue that the owner pins once.

**Owner's words (verbatim):**
> So will I be able to actually view like a dashboard or something that I can view like an interface or whatever of what have been done, what needs to be done, what are we doing now, what are the prerequisites of some things just like this of the actual tracker. [...] And can we do it in GitHub? So it's there in the home screen itself and we can view it in a way, not home screen, in the repository screen. [...] I'm not asking you to develop something new for tracking, but use the actual thing that we are using for tracking now. But actually like it's, it's gathered in one place shown to me or, or the owner or who's, whoever is going to use agent workflow
> so we have it already but not appering in github, how to see it then ? what it is showing ? a md file ? html ? or what ? how this gui is displayed and what it is looks like ?
> yes go ahead, build the pinned status issue

**Restated:** Show the existing tracker (the records and the pull requests) in one place the owner can open on GitHub, without a second tracker.

**Evidence (verified 2026-09-25):** `wf status` (IDEA-08) already renders the records as Markdown but only prints it in a terminal, and the adopted project's `AGENTS.md` did not list the command. GitHub's repository home page shows only the README; a committed `STATUS.md` would need a bot push to the protected branch or a pull request per update; a GitHub Projects board needs an issue per task, which is a second tracker. The project launcher `scripts/wf` reads the pin from `docs/workflow/config.json` and fetches the public workflow repository itself, so an Action can run it without secrets.

**Implemented locally (no control weakened; unreleased until reviewed, approved, versioned and adopted):**
1. `templates/github/wf-status.yml`: runs on pushes to the trusted branch, every six hours and on demand, and acts only on the trusted branch; never on pull requests; permissions `contents: read`, `issues: write` and `pull-requests: read`; actions pinned to the same full hashes as this repository's CI. It checks out the full history, since readiness previews check each task's recorded revisions against it, and runs `scripts/wf status` at the pushed commit in a step that holds no token; the publishing step adds the open pull requests and a link to the merged ones, cuts the body at a line boundary before GitHub's size limit, and edits the open issue labelled `wf-status` or opens it. Cost removed: the owner no longer needs a terminal or an agent to see the status. Recurrence check: `validator/test/status-workflow.test.js` (triggers, branch guard, the exact permissions, pinned actions, no persisted credentials, full history, no expressions in scripts, no token on the rendering step), observed failing for a pull-request trigger, write access to contents, an unpinned action, a removed branch guard, a shallow checkout, an expression in a script and a token on the rendering step. The GitHub side cannot be exercised here: both steps were run locally against demonstration projects with a stand-in `gh` (the first run opened the issue, the second edited it, an oversized body was cut, and with recorded task revisions the previews were correct only with full history), and the owner's first run is the live check.
2. `bin/wf-adopt` installs it when absent, with the trusted branch filled in and checked before the first write; adds the owner's pin step to the checklist; and lists `status` and the issue in the generated `AGENTS.md`. `procedures/setup.md` steps 3 and 5, `procedures/operate.md` (an upgrading project copies the file itself, since the scaffold runs only once), `QUICKSTART.md`, `SCHEMA.md`, `AGENTS.md` and `README.md` describe it. Recurrence check: `validator/test/adopt.test.js`, observed failing without the scaffold change. The installed file is under `.github/**`, an enforcement path, so changing it needs the owner's review.

**Independent review (2026-09-25, separate-context Claude Code subagent per `procedures/review.md`, posted on the pull request):** *changes required*. One blocking finding: the checkout fetched one commit, so every open task with recorded revisions would have been published as blocked with false reasons; the workflow now fetches the full history and a test asserts it. One should-fix finding: the file claimed that candidate code could never write the issue, but a collaborator's own workflow can; the claim now covers this workflow only, and the limits below and setup step 3 call the issue a convenience view. Notes adopted: no expressions in scripts (tested), the token kept off the rendering step (tested), the schedule's default-branch condition, the trusted branch set in two places, and wording in `AGENTS.md`, the generated `AGENTS.md` and `QUICKSTART.md`. Notes accepted as they are: a cancelled run can leave a second labelled issue until the next run edits one of them, and pull request titles and record text render as Markdown in the issue. The same reviewer re-checked the fixes at `08f100d`, confirmed each one, and changed the result to *approve with notes*.

**Limits:** a convenience view, not a control: nothing in the issue approves anything, the gates read the records, and a collaborator's own workflow could edit it, so check consequential items against the records and pull requests. It updates on each push and, when the trusted branch is the default branch, every six hours; it is not live; the open pull request list is a snapshot, and GitHub's own list is one click away. On a public repository the issue is public. The agent's usage and claims stay in the private runtime state. Task history is the merged pull requests, since task records are removed at merge. If someone closes the issue, the next run opens a new one to pin. The schedule uses about 120 minutes of Actions time a month, which counts only for private repositories. PrintFlow gets it when it moves its pin and copies the file.

**Corpus size (per the S7 rule):** IDEA-10's formula, `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`: 18,520 before (at `5584540`) and 18,724 after the review fixes, a growth of 204 words (1.1%). The workflow file (`templates/github/`) is outside that formula and is not read by agents.

---

## IDEA-13 — Two people, each with their own agents, in one repository

**Status:** Implemented, released in `v1.3.0` (2026-09-26, pull request #12 and its release pull request), after a consultation with Fable and an independent review (both below). Implemented: a claim on GitHub that only one agent can win, a check that no record ID is reused, people recorded in the profile and milestones, a status view that shows what waits on each person and each task's claim, and the setup, identity and approval arrangement for a shared project. Needs owner decision: four items below. Noticed and left for a separate task: a removed task record cannot satisfy another task's prerequisite (now IDEA-14 and IDEA-15).

**Owner's words (verbatim):**
> What if me and my friend want to actually work on the same project and both use agent-workflow. He is also using claude code. We are having the same repo and i am as collaborator.
> So how we are going to work ? . Not that I could prepare the plan and the tasks but my friend's agents are going to implement them and vice versa . Did you get it ?
> So is the current agen-workflow ready for this or not yet and we need to make some changes?
> However know what needs to be done and do it .
> If you have any further questions please feel free.
> I will go and sleep now , see yah if I woke up.

**Restated:** Two people share one repository, each running their own Claude Code agents. Either can plan a milestone and its tasks for the other's agents to implement. Is the workflow ready for that, and if not, make it ready. Reading of "i am as collaborator": the repository is under the friend's personal account and the owner has write access; the changes work either way.

**Evidence found (verified 2026-09-25 at `e7f5c79`, and by Fable independently):**
- Claims are per machine. `procedures/execute.md` has the coordinator serialise claims in the private run record; `procedures/operations.md` supports "one host and one coordinator" and one active claim per runtime directory (`validator/lib/runtime.js`). Two people's agents cannot see each other's claims, so both can start one task.
- IDs are chosen locally and task records are removed at merge (`AGENTS.md`). Two planners pick the same next number, and a planner reading `docs/workflow/tasks/` can reuse an ID the history already used. Reproduced: a stale branch that adds `T-0007.md` merges without a conflict after another `T-0007` was planned, merged and removed. Nothing in `validator/` read history.
- "The owner" is singular in the status view: acceptance and release items wait on the literal `owner` (`validator/lib/status.js`), task lines do not show who a task is assigned to, and the open pull requests are listed without their tasks. Milestones had no field for the person who authorises and accepts them. Decisions already name an `owner`; a task's `owner` is the implementer identity that a manual-mode review receipt must match (`validator/lib/lifecycle.js`).
- Who approves what between two people was unstated. With both people in `CODEOWNERS`, either approval passes, so a person can approve their own agents' change to the other person's milestone or to the shared rules. GitHub can require one named person per file pattern, but not "whoever asked" or "whoever did not write it", and (Fable) a higher approval count does not force both people to approve, since worker accounts have write access and can approve.
- Identity is written for one owner and one worker (POLICY § 7, `procedures/identity.md`, setup step 2, the scaffold checklist). The identity adapter itself works unchanged for the friend's repository and worker (`adapters/identity.mjs` only requires a user-owned repository). On a personal repository only the repository owner can change rulesets, collaborators and Actions settings.
- Budgets and ledgers are per host; central reporting has one `worker` and publishes worker activity to `Cazy00/agent-workflow` (Fable). Manual mode verifies one key (`validator/lib/trust.js`).
- POLICY § 7 keeps the repository "under the owner's personal GitHub account"; with the friend's repository this is a collaborator arrangement the policy did not foresee.
- (Fable) Editing a milestone record makes its active tasks' readiness stale (`validator/lib/freshness.js`), which is exactly what happens when one person adds work while the other's agents are implementing.

**Consultation (2026-09-25):** Fable, in a separate read-only context given the owner's words, the gap list and nine proposed changes, fact-checked each gap against the files and gave a verdict per change. Adopted from it: the claim is a push to a new branch named exactly after the task, which Git accepts only once (made create-only after the review, below), with the lowest open pull request number as the tie-break for other branch names (the brief had only the tie-break); one coordinator per milestone, its owner's agent, with the other person's agents implementing only tasks assigned to them, which keeps POLICY § 1's "one coordinating agent per active milestone" rather than re-opening IDEA-11's "not now: parallel coordinators"; reading the ID history along the trusted branch's first parents (reversed after the review, below); the task `owner` meaning written into `SCHEMA.md`; the status view reduced to per-person waiting items, the claim on each task line and the flags; the approval-count option corrected and per-file `CODEOWNERS` offered instead; discovery tests per person; the central-reporting consent line; and the sentence on milestone edits staling active tasks. Two disagreements, both settled: Fable first proposed reporting a shallow clone as `unverified`; the coordinator argued that a check that cannot run is a blocker in this workflow, that `wf ci` already needs history to find the baseline, and that the fix is `fetch-depth: 0`, and Fable withdrew it, asking that a shallow clone exit 2 (an execution error) and a reuse exit 1 (a verdict). Fable suggested flagging a revert of a removal; the coordinator kept an exception for the same record restored, and Fable agreed, asking that the restoration be shown to the reviewer. Fable's further refinements were adopted: "no pull request" only on Ready and Active lines, an Active task with no pull request flagged, and a pull request whose branch and title name different tasks reported rather than guessed.

**Implemented locally (no control weakened; unreleased until reviewed, approved, versioned and adopted):**
1. **Claim on GitHub** (`procedures/execute.md`, *More than one person*). With two or more `owners`, each milestone has one coordinator, its owner's agent, which assigns each task's `owner`; other agents implement only their person's tasks, and until the owners decide otherwise each person coordinates at most one active milestone. An agent claims a task by committing its record with the start facts and status `Active` and creating a branch named exactly the task ID with a push that fails if the branch exists (`git push --force-with-lease=refs/heads/T-NNNN: ...`), then opening a draft pull request from it. A rejected push means the branch exists or the push failed: stop, report the error, never pull, rebase or build on that branch. The claim branch holds the claim; a pull request on another branch holds it only when no claim branch exists, the lowest-numbered first. The holder releases a claim by closing its pull request and deleting the branch. Failure prevented: two people's agents implementing one task. Recurrence check: Git's atomic ref creation, verified on a local bare repository (the create-only push created a missing branch and was rejected when the branch existed, even for a commit built on it, where a plain push was accepted); the status view flags a task claimed twice, a claim branch without a pull request and an `Active` task with neither (`validator/test/status.test.js`).
2. **No reused IDs** (`validator/lib/ci.js`, `validator/lib/sources.js`). `wf ci` fails when a candidate adds a task, decision, milestone or feedback record whose path any commit in the trusted branch's history held with a different identity (title; question; outcome; task and rule for feedback), read with `--full-history` so a use on a merged or fast-forwarded branch counts; a record whose every earlier version had its identity passes as a restoration, with a note; a changed identity on an existing record, such as an add/add conflict resolved by keeping the other side, is noted; a shallow clone exits 2. Failure prevented: two pieces of work under one ID, which the pull requests (the permanent record) and the claim branches would then confuse. Recurrence check: `validator/test/record-ids.test.js` (reuse after removal, an ID used only inside a merged branch, one whose use reached the trusted branch through a fast-forward over a merge, and a candidate matching only one of two earlier identities all fail; a new ID and a restoration pass; a changed title is noted; a shallow clone exits 2), observed failing without the check, without `--full-history` for the merged and fast-forwarded cases, and when comparing with the latest version only.
3. **People in the records** (`validator/lib/records.js`, `templates/profile.md`, `templates/milestone.md`, `SCHEMA.md`). Optional profile `owners`, which must be a list of two or more distinct GitHub usernames; with it, every task's `owner` and every milestone's new `owner` must be one of them, checked by `wf records` and `wf ci`. Failure prevented: work assigned to a misspelt or absent person that no one's agents pick up, acceptance waiting on no one, and a malformed `owners` silently turning the shared view off. Recurrence check: `validator/test/status.test.js` (an owner outside the list, a milestone without an owner and four malformed `owners` values are record errors; `wf records` exits 1).
4. **Status by person, with claims** (`validator/lib/status.js`, `validator/cli.js`, `templates/github/wf-status.yml`). With two or more `owners`, one "Waiting on" section per person (milestone acceptance and release on the milestone's owner, decisions on their named owners, anything else on either owner). Given `--pull-requests FILE`, the JSON of `gh pr list`, same-repository pull requests map to tasks by the CLI's branch rule or a title beginning with the ID; a task pull request ready for review waits on its milestone's owner, changes requested go to the task owner's agents, and the flags above appear. In a shared project, claim branches are read from the checkout's `origin` branches. Pull request titles appear only in code spans, so no link, mention or HTML renders, and fork pull requests appear only in the list, never in anyone's actions. The status workflow gains a step that lists the pull requests with the token; rendering still holds no token, and publishing no longer lists pull requests itself. Cost removed: each person sees what waits on them and what the other's agents are doing without asking. Recurrence check: `validator/test/status.test.js`; `validator/test/status-workflow.test.js` now allows the token on exactly the listing and publishing steps and requires listing before rendering, observed failing for a token on the rendering step, rendering before listing and a write command in the listing step; the status tests fail without the code spans, with forks in the action lists, and with the lowest number always holding; the three steps were run locally against a scaffolded shared project with a stand-in `gh`, and the issue body showed the review waiting on the milestone owner.
5. **Approval between people** (`procedures/setup.md`, *Shared project*; `templates/profile.md` *Authority*). `CODEOWNERS` assigns every path to the people and no worker, with code-owner review and approval of the most recent reviewable push required, and the approval-path test shows that a worker's approval cannot merge. Default rule: whoever is asked to do work approves the plan that assigns it, the milestone's owner approves the result, and a change to the profile, config, `CODEOWNERS`, required checks or workflow files is approved by the person whose agent did not write it; the profile records who approves when one person is away. Failures prevented: one person's worker approving the other's pull request, which GitHub counts toward the required approvals unless code-owner review covers the path; and a person approving their own agents' change to the other person's plan or to the shared rules. Recurrence check: the approval-path test (setup step 9) for the first; none mechanical for the rule itself: GitHub checks only that a code owner approved who neither opened the pull request nor pushed to it last, and naming one person in `CODEOWNERS` for a milestone or decision file enforces it for that file.
6. **Setup and identity per person** (`procedures/setup.md` step 2 and *Shared project*, `procedures/identity.md`, `procedures/operations.md`, `procedures/accept-release.md`, `templates/setup.md`, `bin/wf-adopt --owner`). The repository stays under one person's account, recorded as an approved deviation; that person grants access and turns on deleting head branches after merge; an adopted project converts in one pull request that sets every owner; each person keeps their own worker, clone, token, identity check, discovery test, share of the limits and operations config, and consents to central reporting or keeps local feedback; manual mode has one key, so one person signs or the project uses enforced mode; the milestone's owner accepts it. The scaffold's `--owner NAME`, once per person, fills `owners`, adds the shared-project checklist line and tells the agents in the generated `AGENTS.md` to work only on their person's tasks and claim each one. Failure prevented: the second person's agents running on the first person's credentials, or a setup that never verifies the second person. Recurrence check: `validator/test/adopt.test.js` (one owner, a bad name and a duplicate are refused; a workflow revision without support, built in the test so no tag is needed, is refused before any write; a one-owner adoption is unchanged), observed failing without the scaffold change and without the support check.

**Independent review (2026-09-26, separate-context Claude Code subagent per `procedures/review.md`, posted on the pull request):** *changes required*. One blocking finding: with two worker accounts, one person's worker could supply the approval that merges the other's pull request on any path `CODEOWNERS` does not cover; `CODEOWNERS` now covers every path, code-owner review and approval of the most recent push are required, and step 9 tests it (change 5). Should-fix findings, all fixed: claim precedence contradicted itself, and the status view named the lowest-numbered pull request even when the claim branch had a higher one (the claim branch now holds, tested); the branch lock accepted a push built on another agent's claim branch, closing a pull request did not release its branch, and nobody was named to delete one (create-only push, release by the holder, the error reported, claim branches in the status view); "a code owner other than the last pusher" overclaimed without the most-recent-push setting (corrected, and required); two active milestones were ambiguous (an interim rule of one per person); the procedure already settled the POLICY § 7 reading the entry called open (now recorded as an approved deviation, which approving this change approves); untrusted pull request titles reached the owner's action list as Markdown (code spans, forks only in the list); a malformed `owners` turned the view off silently (now a record error); two scaffold checks skipped silently without tags (both unconditional); and converting an adopted project was undescribed (one pull request that sets every owner). Notes: `--first-parent` was untested and, the implementer then found, missed a use that reached the trusted branch through a fast-forward over a merge, as does Git's default history view, so the check reads `--full-history` (tested); one identity field misses a same-title reuse and an add/add conflict resolved the wrong way (identity changes on existing records are now noted, and feedback compares task and rule); the adoption note understated the one-owner changes and the upgrade step lacked `fetch-depth: 0` (both corrected); duplicated branch rule, record directories and error matching (shared now); the corpus figure (corrected). Accepted as limits: below.

**Re-check (2026-09-26, the same reviewer, at `f8db0bf`):** *approve with notes*. Every finding resolved or recorded as a limit, with ten mutations of the fixes each caught; the `--full-history` reasoning confirmed by rebuilding the fast-forward case. One new should-fix finding: `git log -1` returned the latest-dated version anywhere in history, so a candidate matching a later use inside a merged branch passed as a restoration although the trusted line held a different identity; the check now compares with every version and passes a restoration only when all of them match (tested, observed failing with the latest version only). Two notes, both fixed: `procedures/execute.md` still said "do not run concurrent coordinators" without the shared-project exception, and `procedures/approval-evidence.md`'s list before switching to enforced named only governing and enforcement paths for `CODEOWNERS`; and claim branches were read in one-owner projects too, where a leftover `T-NNNN` branch is no claim (now read only with two or more owners, tested). The reviewer also noted that approving this change approves two items listed below: the repository under the friend's account, recorded as a deviation, and the interim rule of one active milestone per person.

**Needs owner decision (nothing implemented):**
- **POLICY § 7 and the friend's repository.** The policy keeps the repository under the owner's personal account. `procedures/setup.md` now records a shared project's repository, under either person's account, as an approved deviation in the profile, and approving this change approves that reading; say instead if the repository should move to your account or to an organisation you both own.
- **Two active milestones at once.** Each person planning their own milestone means two active milestones, each with one coordinator. POLICY § 1 allows one coordinator per active milestone, and IDEA-11's "not now: parallel coordinators" was about an unattended runner and scale. `procedures/execute.md` now sets an interim rule, at most one active milestone per person, until you decide otherwise in the profile.
- **Who approves when one person is away.** Under the default rule an absent person blocks every result they asked for and every shared-rule change; waiting is never approval, so only the owners can name the fallback.
- **Enforcing part of the approval rule.** Per-file `CODEOWNERS` entries for milestone and decision records make the named person's approval mandatory for those files, at the cost of a `CODEOWNERS` change (an enforcement path) per milestone. Not done by default.

**Noticed, not changed (a separate task; within a milestone, IDEA-14 keeps records until acceptance instead):** a task record removed at merge can no longer satisfy another task's `prerequisites` (`validator/lib/readiness.js` requires it Done on the baseline), and a candidate cannot drop a trusted prerequisite either, so a dependent task needs an approved planning change after its prerequisite merges. This affects one-person projects equally. Options: accept a prerequisite whose last version in the trusted branch's history was Done (reusing change 2's history lookup), or remove a record only once nothing references it. Taken up as IDEA-15.

**Limits:** the approval rule is kept by the people, not by GitHub. The claim is atomic only on the exact `T-NNNN` branch; a harness that forces a branch prefix cannot push it, and falls back on the lowest-numbered pull request, which needs each agent to re-check. The status issue is not live (each push to the trusted branch and every six hours), sees the claim branches its checkout fetched, and lists at most 500 open pull requests. With `owners`, a manual-mode review receipt is checked against the person, not the worker, so a receipt naming the implementing worker as reviewer passes; receipts are signed only after the collector's review. No record maps a worker to its person, so a planning or shared-rule pull request waits on either owner; each person's share of a milestone's limits lives in their own operations config, so nobody sees the total; `approval.approver` stays single-valued; stacked pull requests for one task show as a duplicate claim. A record's identity is its title (question, outcome; task and rule for feedback), so different work under an old ID with the same title passes as a restoration. The ID check walks the history once per added record and needs the full history. Nothing here was exercised with two real accounts or on GitHub; the owners' setup steps and first shared milestone are the live check.

**Adoption note:** `owners` is optional. A one-owner project that moves to this version gets two changes: `wf ci` reads the history when a record is added, so a CI checkout must fetch the full history (`fetch-depth: 0`; `procedures/operate.md` now says so), and, once it copies the new status workflow (from the same release as the pin, since it passes `--pull-requests`), the status issue marks each open task's pull request or its absence, lists non-draft pull requests under "Waiting on the owner" and flags claims.

**Corpus size (per the S7 rule):** IDEA-10's formula, `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`: 18,733 before (at `e7f5c79`) and 19,746 after the review fixes, a growth of 1,013 words (5.4%). Most of it is the claim rule and the shared-project section, which a one-owner project can skip; the review fixes added about 230 of those words.

---

## IDEA-14 — Plan one milestone at a time, and keep work discovered along the way

**Status:** Implemented (unreleased), after a consultation with Fable (below). Needs owner decision: the timing of removing task records.

**Owner's words (verbatim, 2026-09-26):**
> So I'm going to give the input for the development. I'm going to give the goals, the prototype. [...] Now it's time for actually starting the development. So there are tasks that are going to be created. So those tasks, are they going to be created while we are working? [...] Or there will be a pre-plan. This pre-plan is not actually concerned about the whole project, but it's only concentrating on one milestone.
> For example, first, we need to know how we are going to actually divide our milestones. Based on what? Is it based on screen? Is it based on a certain feature? Is it based on what exactly?
> [...] Or we look at a certain milestone, take it, make a full plan of that milestone and lay it down in the tasks. And we are actually also writing that those tasks are serving a certain milestone, for example. And if there are maybe any extra work that needs to be done, the agent can be actually create other tasks. [...] down the line we actually saw or we knew about something that we haven't actually include or we haven't actually write in the plan. From there, there can be a subtask or a task that can be created maybe later on, like a progressive task to actually complete the work instead of actually going by the plan and actually forget that one.
> Okay, that's nice. Okay, bring this idea or those ideas to Fable and see what he have to say about it and actually see what we can implement right now and let's start implementing it in a new branch.

**Restated:** How are milestones cut, is each milestone planned into tasks up front or are tasks created as work goes, and what happens to work an agent discovers that the plan did not include, so that it is neither done outside the plan's authority nor forgotten.

**Evidence found (verified 2026-09-26 at `3357e3c`, and by Fable independently):**
- The shape of a milestone and the one-at-a-time rhythm were already there: "one coherent journey or a clearly demonstrable technical outcome" (POLICY § 6), a thin end-to-end first milestone (`procedures/readiness.md`), acceptance IDs for the first milestone only (setup step 7), the next milestone recommended at acceptance. Nothing said what to cut a milestone *by*.
- Tasks were already bounded by their milestone: `wf readiness` fails a task whose scope or acceptance IDs fall outside its milestone's (`validator/lib/readiness.js`), and milestone authorisation lets agents organise tasks within it (POLICY § 6). So a task an agent adds mid-milestone could never widen the milestone.
- No milestone sequence existed, although POLICY § 2 puts "Milestones" in the profile's *Delivery* area; `templates/profile.md` had dropped the word (Fable).
- No procedure said when a milestone is broken into tasks or that each acceptance scenario needs a task. The milestone's `tasks:` field was read by nothing (Fable). Nothing checked milestone coverage mechanically at any stage: `wf lifecycle --stage accept` checks the task's acceptance IDs, not the milestone's (Fable).
- No rule covered work found during a task: execute had only "fix in-scope defects". `procedures/operate.md` already had the rule for operations findings ("a task under an authorised milestone, or a decision when it changes scope"), and POLICY § 5 covers found decisions (Fable).
- Planned prerequisites could not work (Fable): readiness needs a prerequisite task `Done` on the trusted branch, `wf ci` refuses a production pull request whose task is `Done`, nothing published said when `Done` is set, and this repository's `AGENTS.md`, the only place the rule appeared, removed task records at merge.
- Constraints: a Draft milestone record needs all twelve fields (`validator/lib/records.js`); any profile edit makes every task's readiness stale, and any milestone edit every task of that milestone (`validator/lib/freshness.js`); adding a task record stales nothing else.

**Consultation (2026-09-26):** Fable, in a separate read-only context given the owner's words, the gaps and eight proposals, fact-checked each against the files and gave a verdict per proposal. Adopted from it: the milestone sequence goes in the profile's *Delivery*, restoring POLICY § 2's word, rather than a new roadmap file (a third place for plans) or sparse Draft milestone records (governing paths, and they blur where authority comes from); work parked for a later milestone is a Draft task naming that milestone's ID, which already validates and already shows in `wf status`; planning belongs in `procedures/readiness.md`; the found-work rule mirrors `operate.md` with three outcomes instead of the coordinator's four; coverage is a status-view flag, not a readiness failure, which would block every task over a planning omission; and the change that makes the rest work is procedural: keep task records, marked `Done`, until the milestone is accepted, instead of a history lookup in the validator. The coordinator added three points and Fable agreed to each: publish when `Done` is set (Fable added "before any task that names it as a prerequisite is assessed"); record that in a shared project a profile edit fails the other person's active tasks and how they recover (Fable corrected "stale" to "fails", and gave the remedy on the task's own branch); fold a Draft's unrecorded start facts into one line and keep them in the JSON (Fable: fold, do not skip, since a Draft may be assessed before promotion).

**Implemented locally (no control weakened; unreleased until reviewed, approved, versioned and adopted):**
1. **How to cut milestones** (`procedures/readiness.md`): by a journey a user completes end to end, across screens, backend and data, that the owner can be shown working, or where no user journey applies by a clearly demonstrable technical outcome (POLICY § 6); not by screen or layer, nor by whole feature. Failure prevented: milestones with no demonstrable outcome, which stall at acceptance. Recurrence check: none mechanical; the required non-empty `demonstration:` is a partial one.
2. **A milestone sequence** (`templates/profile.md` *Delivery*): one line per milestone, a forecast, no authority; edits batched with the next milestone's planning change. Failure prevented: nothing sketching the project beyond the next milestone, so work for later has nowhere to go. Recurrence check: none mechanical (prose template).
3. **Plan the milestone** (`procedures/readiness.md`, `templates/milestone.md`, `SCHEMA.md`): before authorisation the coordinator writes the milestone's tasks as Drafts, each serving acceptance IDs so that every ID has a task, lists them in `tasks:` and orders them; Drafts already parked for the milestone join the plan; the owner authorises the milestone with its plan; `tasks:` stays the plan as authorised, and dropping a planned task is a batched milestone edit. Failure prevented: a scenario no task serves, found only at acceptance. Recurrence check: change 6.
4. **Work found along the way** (`procedures/execute.md`): inside this task, do it here and say so; needed for this milestone, a Draft task with the same `milestone:` and a line `Found in T-NNNN: <reason>`, not added to `tasks:` so no active task goes stale; anything else, a Draft task naming its later milestone, or a decision when it changes scope, behaviour or acceptance. Tasks stay flat. In a shared project the implementing agent writes the Draft with the owner of the milestone it names as `owner` (of the milestone it was found in while the named one has no record), so that person approves the change that adds it and the coordinator assigns it, and an agent claims a task only once its coordinator has made it Ready; IDEA-13's approval rule is unchanged. The sentence on numbering new records above every ID in history moved out of the shared-project section, since the reuse check applies to one-person projects too. Failure prevented: found work forgotten, or done without authority. Recurrence check: the existing readiness bounds (scope and acceptance IDs inside the milestone) for the second; review's "behaviour beyond scope" finding for the first; nothing mechanical can detect work that was never written down, so change 6 and change 7 make what was written visible.
5. **`Done`, and when records go** (`procedures/execute.md`, `AGENTS.md`, comments in `validator/lib/ci.js` and `validator/test/record-ids.test.js`): after a task merges, the coordinator marks it `Done` in a planning change, which may batch several, before any dependent task is assessed; `Done` records stay until the milestone is accepted, then go in one planning change; a task in another milestone depends on a `contract:`, not on a task, and a record an open task still names stays until that task's milestone is accepted; in this repository, a task that belongs to no milestone goes once merged. Failure prevented: prerequisites between planned tasks failing as soon as the first merges (the IDEA-13 noticed item, within a milestone); coverage and discovered work becoming unknowable mid-milestone. Recurrence check: the existing prerequisite rule in `readiness.js`, which now has records to read and fails a dependent task whose prerequisite record is gone.
6. **Reassessing after a profile or milestone edit** (`procedures/execute.md`, *More than one person*): an edit fails the readiness of the active tasks it governs, and the implementing agent reassesses on its own branch by reading what changed in its governing records since the recorded revision, then recording the new `governing_baseline_revision` and rerunning the gate. Failure prevented: another person's active tasks left failing with no stated recovery, or "recovered" by moving the revision without reading the change. Recurrence check: the freshness check fails the task until its revision moves; the re-read itself is not mechanical, and review checks it.
7. **The plan in `wf status`** (`validator/lib/status.js`): per milestone, `plan` with the planned, discovered, unserved and missing IDs; the header shows "planned n · discovered m" (duplicates counted once) and each discovered task is marked; until acceptance, each acceptance ID no task serves and each planned task with no record is an item for the milestone owner's agents; a Draft's unrecorded start facts fold into one line, kept in `missing_fields`; "Tasks without a known milestone" becomes "Tasks without a milestone record", and a task with none reads "no milestone"; an `Active` task with no claim says the coordinator marks it `Done` if its pull request merged. Failure prevented: an unserved scenario or a lost planned task going unseen; a planned milestone reading as a wall of blockers. Recurrence check: six new tests in `validator/test/status.test.js`, five observed failing before the code they test and one pinning attribution that already held, plus the changed message in an existing test; every mutation the implementer and the reviewer tried is caught: raising plan items for an accepted milestone, folding a Ready task's reasons, marking tasks discovered when no plan exists (the implementer's), and `Done` tasks no longer serving acceptance IDs, plan items only for Authorised or Active milestones, `Released` not excluded, plan items naming no person, a `Done` planned task counted as missing, no de-duplication, the old heading, and no plan items for Blocked or Verified milestones (the reviewer's).
8. **The acceptance package** (`templates/acceptance.md`, `procedures/accept-release.md`): planned tasks, discovered tasks and work moved to later milestones, each with its pull request. Failure prevented: the owner accepting without seeing how the plan changed. Recurrence check: none mechanical (prose template); `wf status --json` supplies the lists.

**Needs owner decision:** keeping task records until the milestone is accepted changes a rule this repository's `AGENTS.md` stated ("remove a task record once its work is merged"). No control weakens: the records are planning paths, and the pull requests stay the permanent record. Approving this change approves the new timing; the alternative, keeping removal at merge, needs the validator to accept a prerequisite from history and leaves coverage and discovered work unknowable mid-milestone.

**Not now:** a roadmap file and Draft milestone records (rejected above); a readiness failure on an unserved scenario (it blocks unrelated tasks); a history-based prerequisite lookup (a separate task only if the owner keeps removal at merge); a `wf ci` note showing coverage on the planning pull request, since the status issue updates only after merge (optional; left).

**Independent review (2026-09-26, separate-context Claude Code subagent per `procedures/review.md`, posted on pull request #15):** *changes required*. One blocking finding: in a shared project a finder's agent wrote found work with its own person as `owner`, so that person would approve their own agent's task in the other person's milestone, which IDEA-13's approval rule exists to prevent; found work now names the milestone's `owner` (change 4). Should-fix findings, all fixed: the cutting rule was narrower than POLICY § 6, which also allows a demonstrable technical outcome (change 1); removing records at acceptance broke a dependency from another milestone, and `contract:` was cited but not written down (change 5); the tests did not pin the behaviours that justify keeping records, and five mutations survived (change 7, now caught); the reassessment wording could pass without reading what changed, and the instruction was missing from this list (change 6). Notes, all adopted: `Done` marks may be batched, and each batch is a pull request the owner merges (limits); the status message for an `Active` task with no claim now mentions marking it `Done`; the heading covers tasks with no milestone; duplicates in `tasks:` count once; parked Drafts join the plan, and dropping a planned task is stated; a task with no milestone is removed once merged in this repository's `AGENTS.md`; the profile template no longer calls a governing edit a planning change.

**Re-check (2026-09-26, the same reviewer, at `5a79841`, posted on pull request #15):** *approve with notes*. Every finding and note resolved, checked against the files and against IDEA-13's claim, approval and assignment rules; ten of eleven mutations caught. Four notes, all adopted: a found Draft's `owner` routes approval but is not yet an assignment, so an agent claims a task only once its coordinator has made it Ready; the status message for an `Active` task with no claim now says the coordinator marks it `Done`; work parked in a shared project for a milestone with no record names the owner of the milestone it was found in; and the surviving mutation, no plan items for Blocked or Verified milestones, is now caught.

**Limits:** coverage is a flag in a derived view, not a gate. The discovered mark needs a filled `tasks:`; with none, nothing is marked. Marking `Done` is a planning pull request after each merge, or one per batch, which the owner merges under setup step 5; dependent tasks already needed it, but nothing had said so. A profile edit fails the readiness of active tasks; in a shared project, the other person's. Nothing ran on GitHub or in an adopted project.

**Adoption note:** a project that moves to this version mid-milestone, having removed records at merge, sees the acceptance IDs and planned tasks of its merged tasks flagged until that milestone is accepted; the flags are informational.

**Corpus size (per the S7 rule):** IDEA-10's formula, `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`: 19,797 before (at `3357e3c`) and 20,423 after the review fixes, a growth of 626 words (3.2%); the review fixes added 126.

---

## IDEA-15 — A removed task record that another task still names

**Status:** Implemented (unreleased) (2026-09-26, pull request #14, republished and merged as #16), after a consultation with Fable and an independent review (both below). Implemented: a task record that another task names in `prerequisites` or `governing` stays, `Done`, until the last record naming it is removed, and `wf ci` fails a change that would leave a name without its record. Opened as IDEA-14 alongside pull request #15, which used the same number and merged first; renumbered and combined with IDEA-14's timing (below). The owner then decided the planning order (below).

**Owner's words (verbatim):**
> In the `Cazy00/agent-workflow` repository [...] fix a readiness defect around removed task records.
> [...] After the prerequisite's record is removed at merge, every task that lists it fails readiness with "prerequisite T-NNNN is not Done on the trusted baseline (missing)". The dependent task cannot simply drop the entry either [...] This hits one-person projects too, and more often when one person plans tasks that another person's agents implement.
> **Two candidate fixes; choose one and justify it:** 1. Code: accept a task prerequisite that is absent from the baseline if its last version in the trusted branch's history had `status: Done`. [...] 2. Zero code: change the rule to remove a task record only once it is merged and nothing references it [...]
> span a sub-agent and ask Fable about this. See what he have to say about it.
> You both try to figure out the best solution.

**Restated:** `AGENTS.md` removes a task record once its work is merged, but readiness needs a task prerequisite present and `Done` on the trusted baseline, and refuses a candidate that drops a trusted prerequisite. Choose between reading `Done` from history and keeping named records, and make a dependent task's route work without a separate planning change.

**Evidence found (verified 2026-09-26 at `3357e3c`, and by Fable independently):**
- `validator/lib/readiness.js` fails a `T-NNNN` prerequisite that is missing or not `Done` on the baseline, and a `governing` task ID that is missing; a candidate cannot drop either (`removed trusted ... reference`). Reproduced on a Git copy of `fixtures/06a-contract-prerequisite`: `Ready` while the prerequisite is present and `Done`; after its removal, `(missing)` even after the dependent reassesses.
- Nothing says when `Done` is set. No procedure or template says who sets it or when; `wf ci` refuses production changes from a `Done` task, so the implementing pull request cannot carry it. Practice here varied: under the rule, `155477c` (pull request #4) removed T-0001 to T-0005, all `Done`, and `a36ed38` (pull request #5) removed T-0006, whose last status was `Active`. The removal rule is in this repository's `AGENTS.md` only; the scaffold's generated `AGENTS.md` does not carry it.
- Reading `Done` from history (option 1) fails both ways. A squash merge of a pull request that marks a task `Done` and then removes it leaves only the earlier status in history (reproduced by both). `git log -1 --full-history --diff-filter=AM` skips merge commits and orders by date, so it can return a side branch's `Done` version that a merge discarded, and a prerequisite the trusted line removed while `Active` would pass (reproduced by Fable; the same class of error as IDEA-13's re-check). A deletion made by a merge commit is invisible without `-m`, and a shallow clone returns nothing with exit 0. Under a merge commit, the `Done` read would be an intermediate branch commit the reviewer saw only as "removed".
- `wf records` and `wf ci` passed a change that removes a task record another task names, and one that adds a task naming a task that does not exist.
- (Fable) Removal at merge also means `wf lifecycle --stage accept|release --task` finds no record for a merged task.

**Choice: option 2, with a check.** Keeping a named record reads `Done` from the approved baseline exactly as `SCHEMA.md` states, weakens nothing, covers `governing` as well as `prerequisites`, needs no history or full clone, and follows POLICY § 3 ("The repository holds only what future work needs to read"). Option 1 would need the same instruction change (land `Done` in its own merge before removing), a history rule with `-m`, deletion parents and every-version comparison, and would still move the `Done` attestation to unreviewed history. Option 2's weak point, a dependent planned on another branch while its prerequisite is removed, is what the check catches; that is why this is not zero code: an instruction alone has no check that could observe the failure (`procedures/maintenance.md`).

**Consultation (2026-09-26):** Fable, in a separate read-only context given the owner's report, both options, the constraints and five questions (when a record becomes `Done` and whether that reaches history under each merge method; what "last version" must mean; other references; option 2's chains and races; a verdict), reached the same choice independently with Git experiments. Adopted from it: a sibling fixture instead of extending `06a`, whose never-`Done` case must stay; the check in `wf ci`, not `validateRecords`, since a record error on the baseline blocks every task; the `-m`, shallow-clone and `lifecycle` findings above. Two refinements from the coordinator, both accepted by Fable: only a named record needs `Done` (Fable first proposed marking every task `Done` in a change of its own, which only option 1 needs), and the check on a changed record fires only on names the change adds, so a name that already dangled does not fail an unrelated edit. Fable's refinements, adopted: the rule separates setting `Done` at merge from removing later; one finding lists every record naming a removed one; the message says "when its work merges".

**Implemented locally (no control weakened; unreleased until reviewed, approved, versioned and adopted):**
1. **The rule** (`AGENTS.md`, `procedures/execute.md`, combined with IDEA-14). IDEA-14 marks a task record `Done` once its work merges and keeps it until its milestone is accepted; this change adds that a record another task's record still names in `prerequisites` or `governing` stays until that record goes, whichever milestone it is in, so a chain goes together. IDEA-14 said "an open task"; a `Done` task whose milestone is not yet accepted still has its prerequisites read by `wf lifecycle --stage accept`, so any remaining record counts, as the check does. Failure prevented: a dependent task blocked by its merged prerequisite's removal, with a governing change as the only route. Recurrence check: change 2.
2. **The check** (`validator/lib/ci.js`, `SCHEMA.md` *Integration and CLI*). `wf ci` fails a change that removes a task record that a remaining task names in either field, listing every such record, or in which a task record newly names a task whose record is not in the candidate. `wf ci` needs the candidate to contain the current baseline and setup step 5 requires up-to-date branches, so a removal and a planning branch naming it meet in whichever merges second. No history is read. Failure prevented: the coordinator removing a named record, and the two-planner race. Recurrence check: `fixtures/06d-removed-prerequisite-record` (a named `Done` prerequisite gives `Ready`; removing it fails; removing an unnamed record passes; a new task naming a missing one fails), whose two failing runs passed before the check; `validator/test/record-references.test.js` (through the CLI on Git: both merge orders of the race, a chain removed together, a `governing` name, two records named in one finding, a naming record without an ID shown by its path, a custom `records_dir`, and an edit of a record whose name already dangled, which passes unless it adds a name), observed failing without the check and, for the dangling case, without the narrowing. Kept: `fixtures/06a-contract-prerequisite` #2 (a prerequisite that was never `Done` blocks) and `validator/lib/readiness.js` unchanged.

**Independent review (2026-09-26, separate-context Claude Code subagent per `procedures/review.md`, posted on the pull request):** *approve with notes*. The reviewer reproduced the failing runs without the check and without the narrowing, the corpus count and the three Git behaviours, and probed a rename, a chain, a removal with a new name in one change, a record without an ID, a branch not up to date (exit 2), a custom `records_dir` and a `governing` path. Two should-fix findings, both fixed: the evidence claimed T-0006 was this repository's only removal, when `155477c` had removed five `Done` records (corrected above); and the status line claimed a review while it was pending (recorded here). Notes adopted: the check returns at once when no task record changed, instead of loading every record on each run (about 45% slower with 303 records); a naming record without an ID is shown by its path, not "undefined" (tested); fixture 06d gains a run listing all three changes. Notes accepted as they are: a removal and a new name in one change report the same cause twice, both true; a task named by path in `governing` is not checked (disclosed below); a failed Git read counts as empty content, as everywhere in the validator. Notes for the owner: the planning constraint and where the rule lives (below).

**Re-check (2026-09-26, the same reviewer, at `b8b20d7`):** *approve with notes*. Every finding resolved or accepted; six mutations of the fixes, five caught. The surviving one, the default records directory written into the early return, is now caught by a test with a custom `records_dir`. One wording note, fixed: the evidence said "Nothing requires `Done`", which readiness does; it now says nothing says when `Done` is set.

**Combination with IDEA-14 (2026-09-26):** the owner first set this pull request to merge first; pull request #15 merged first instead, so this one took the next number and combined the rules, as agreed. The session that opened #15 proposed the same combination independently. IDEA-14 settled the two questions this entry had left to the owner, and the owner approved them by merging it: records stay until their milestone is accepted, so `wf lifecycle --stage accept|release` finds them, and the rule lives in `procedures/execute.md`, which every project reads. `AGENTS.md` takes IDEA-14's sentence with "but never while another task still names it"; the comments in `validator/lib/ci.js` and `validator/test/record-ids.test.js` take IDEA-14's wording.

**Settled by IDEA-14:**
- **When a record goes.** `procedures/accept-release.md` has `wf lifecycle --stage accept|release` read the task, which removal at merge made impossible. Records now stay until their milestone is accepted.
- **Where the rule lives.** It was in this repository's `AGENTS.md` only, and this repository keeps no task records, so adopting projects had the check but no instruction. It is now in `procedures/execute.md`, which every project reads.

**Planning order (owner decision, 2026-09-26):** `wf ci` fails a planning change that names a task whose record is not yet in the candidate, so a planner adds a prerequisite's record before or with the record naming it. `procedures/execute.md` said so; at the owner's request ("add it to readiness.md too") `procedures/readiness.md`, where the coordinator plans a milestone's tasks and their prerequisites, now says so as well, in a follow-up pull request. Recurrence check: the check itself (`validator/test/record-references.test.js`, fixture 06d run #4), which fails a planning change that breaks the rule whether or not the planner read either procedure. Corpus: 20,443 at `062e3d4` and 20,471 after (+28 words).

**Limits:** the rule is kept by the coordinator; the check sees only what a pull request contains, so the race is caught only with up-to-date branches required. A task named by a planning pull request must be in the candidate: a planner adds a prerequisite's record before or with the record naming it. Records stay for the length of a chain. A name that dangled before this version is not flagged by `wf ci`; readiness still blocks that dependent, and restoring the record at its `Done` version (the ID check passes a restoration with a note) or a planning change dropping the name repairs it. A dependent still reassesses once its prerequisite's record changes (`validator/lib/freshness.js`), by design. Only task IDs in `prerequisites` and `governing` are checked.

**Adoption note:** a project that moves to this version gets one change: `wf ci` fails a change that removes a named task record or newly names a missing one. It reads no history.

**Corpus size (per the S7 rule):** IDEA-10's formula, `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`: 19,746 before (at `3357e3c`) and 19,813 after, a growth of 67 words (0.3%); combined with IDEA-14, 20,370 at `f6f9a39` and 20,443 after, a growth of 73 words (0.4%).

---

## IDEA-16 — The shared corpus keeps growing

**Status:** Evaluated 2026-09-26 after a consultation with Fable (below): not a cost problem; one structural pass drafted, unreleased and awaiting independent review and owner approval. Options 1 and 2 taken in reduced form, 3 and 4 declined.

**Owner's words (verbatim):**
> growing corpus - is that a good thing ?
> okay lets highlight this and add it to ideas backlogs we might do some cost optimisation if needed

**Restated:** Every change records the shared corpus's word count (S7, `procedures/maintenance.md`: "so growth stays a visible cost"), but nothing limits the growth or pays it back. Decide whether the corpus should be trimmed, and whether growth needs a budget.

**Evidence (measured 2026-09-26 with IDEA-10's formula, `cat POLICY.md SCHEMA.md QUICKSTART.md AGENTS.md procedures/*.md templates/*.md .agents/skills/workflow/SKILL.md | wc -w`, on `git archive` copies of each revision):**

| Revision | Point | Words |
|---|---|---|
| `58ae362` | before IDEA-10 | 14,685 |
| `7d05561` | before IDEA-11 | 18,060 |
| `5584540` | before IDEA-12 | 18,520 |
| `e7f5c79` | before IDEA-13 | 18,733 |
| `3357e3c` | after IDEA-13 (`v1.3.0`) | 19,746 |
| `f6f9a39` | after IDEA-14 | 20,370 |
| `5b2d6f4` | after IDEA-15 and its follow-up | 20,471 |

- Growth of 5,786 words (39%) in about two days of changes, the largest single step IDEA-10's (its entry records +3,378 from 14,724; the same formula now gives 14,685 at `58ae362`). No change so far has removed words to offset what it added.
- `POLICY.md` (4,914 words) is the owner's and stays verbatim, so the rest, about 15,550 words, is where any trimming would come from.
- `.agents/skills/workflow/SKILL.md` budgets a session at "about three thousand words" (the repository guide, one procedure and the current records). Three procedures now use more than half of that on their own: `procedures/setup.md` 1,756, `procedures/operations.md` 1,617, `procedures/execute.md` 1,611; `SCHEMA.md` is 2,453 and `QUICKSTART.md` 1,177.
- Restatement has a price beyond words: IDEA-15's combination found IDEA-14's "a record an open task still names" in `procedures/execute.md` narrower than the `wf ci` check it describes, and that rule now appears in `AGENTS.md`, `procedures/execute.md` and `procedures/readiness.md`.
- The Super System template that IDEA-09 compared caps its notes at twenty and retires one to add the twenty-first (S7); only the "name the failure prevented" half was adopted here, not the cap.

**Why growth costs:** each session reads more before it acts, which is the adoption cost IDEA-06 recorded; every restatement is a place where two texts can drift apart; and the owner, reviewers and agents all read the same words. Growth is worth it when the added words prevent a recurring failure that no check can catch. A rule a check enforces needs only a pointer to the check (IDEA-11, "checks, not prompts").

**Options for the evaluator (none decided):**
1. **Measure what a session reads.** Report each procedure's size against the session budget, not only the total; a procedure over about 1,500 words is due for trimming.
2. **A pruning pass** under `procedures/maintenance.md`: remove restatements, keep one home per rule with pointers elsewhere, and cut to one line each rule a check already enforces. Candidates: `procedures/setup.md`, `procedures/operations.md`, `procedures/execute.md`, `SCHEMA.md`. Needs a target, such as back under 19,000 words, and must weaken no gate (fixtures and tests unchanged).
3. **A budget:** a change that adds words removes as many elsewhere unless the owner approves the growth explicitly, with the corpus figure already recorded per change as its evidence.
4. **A mechanical check:** a test that fails when a procedure exceeds its budget, so growth is a decision rather than a drift.

**Limits of the measure:** word count is a proxy. It ignores what a session actually opens (only the guide, one procedure and the records), counts templates that most sessions never read, and says nothing about clarity; a shorter text can be harder to follow. The formula leaves out files agents read, such as the reviewer prompt (`templates/claude/agents/independent-reviewer.md`) and the generated `AGENTS.md`.

**Owner's follow-up (verbatim, 2026-09-26):**
> I would like that you check on the ideas backlog and see the latest idea that have been appended there. [...] Is it a concern? Do we need to do something about it? If we, for example, going to optimize this, is it going to actually improve the current? Workflow [...] will it's going to affect the current workflow negatively? So if it is not a big deal and it's not really doing any issue for now, we could keep it. But if there is a room for improvement in the actual performance of this AI agent workflow, we would like to explore our options and you could actually span a sub-agent discussing this with Fable as well
> record it in IDEA-16 and draft the restructuring PR

**Evaluation (2026-09-26, at `9cb4283`):**
- **The total is the wrong measure.** A session reads the repository guide, the skill, one procedure and its records. Measured with the scaffold's generated `AGENTS.md` (about 250 words) and a filled profile of 800 to 1,200 words: an implementation session read 3,500 to 4,000 words (about 6,000 if it followed every pointer in `procedures/execute.md`), setup about 3,550, readiness 2,500 to 3,000. That is 3 to 5% of a 200,000-token context, less than a typical diff or test run. `POLICY.md` has not changed since IDEA-10. No recorded failure is attributed to instruction length; IDEA-06's exhausted allowance came from actions, not reading.
- **Growth landed on the most-read path.** Since `58ae362`: `procedures/setup.md` +1,128 words, `procedures/execute.md` +881, `procedures/readiness.md` +403; `SCHEMA.md` +720 and `QUICKSTART.md` +417 are consulted, not loaded. `procedures/execute.md` had two headings and a 390-word paragraph (the claim protocol for shared projects) that every one-owner session read.
- **The real costs are findability and drift, not tokens.** Found: `QUICKSTART.md` step 4 told every project to provision the owner key and signed receipts, while setup step 6 and `procedures/approval-evidence.md` make `enforced` mode, which needs no key, the default, and `QUICKSTART.md` itself says so further down; `AGENTS.md` removes a Done task that belongs to no milestone at once, which `procedures/execute.md` did not say; `.agents/skills/workflow/SKILL.md` still said "about three thousand words".
- **Trimming has a real risk.** `npm test` checks the validator and the scaffold, never the procedures' text. Rules that live only in prose (delegates never approve or sign, no auth bypass to complete a check, a failing run before acceptance evidence, no replayed side effects, the setup stop rule) have no mechanical guard, so a cut could drop one silently.
- **Verdict per option.** 1, *measure what a session reads*: yes, one clause in the S7 rule. 2, *a pruning pass*: yes, as a structure-and-drift pass that removes no rule, not a word target. 3, *a word budget per change*: no; owner approval already gates every change, and a budget rewards denser sentences, the failure found above. 4, *a failing size check*: no; it would block legitimate changes and can be met by compression. Leaving the corpus as it is would also have been defensible.

**Consultation (2026-09-26):** Fable, in a separate read-only context given the owner's question, this entry and the four options but not the coordinator's view, traced what each session loads, reached the same verdict on all four options independently, and found the three drifts above. Adopted from it: measuring the path a session reads, not the total; no word budget and no size check; fixing the drifts in the same pass; the pull request mapping every moved sentence to its new home. The coordinator went one step further than Fable's headings-only proposal: the shared-project text moved to a procedure of its own, since a one-owner project, the common case, never needs it.

**Drafted (unreleased; independent review and owner approval pending):**
1. **`procedures/shared.md`** holds the rules for two or more owners, from `procedures/setup.md` *Shared project* and `procedures/execute.md` *More than one person*, moved verbatim apart from "setup step 8/9" for "step 8/9", "Each milestone" for "When the profile lists two or more `owners`, each milestone", and the long paragraph split under *Who selects work*, *Claiming a task* and *Changing the plan*. Both old sections remain as one-line pointers, so an adopted project's generated `AGENTS.md` still leads to the rule. Pointers updated in `AGENTS.md`, `README.md`, `QUICKSTART.md`, `.agents/skills/workflow/SKILL.md`, `procedures/identity.md`, `templates/profile.md`, `bin/wf-adopt` (the shared checklist line and the generated guide) and a comment in `validator/lib/records.js`. Cost removed: a one-owner session reads 282 fewer words in `procedures/execute.md` (1,611 to 1,329) and 293 fewer in `procedures/setup.md` (1,756 to 1,463); a shared-project session reads about 100 more (the headings and the new file's opening).
2. **Headings in `procedures/execute.md`** for each phase of a session: *Coordinate and delegate*, *Implement a task* (with **Tests**, **UI**, **Checks** and **Bug fixes** leads), *Work found along the way*, *Checkpoints and task records*, *Resume, limits and handoff*. In `procedures/setup.md` the *Stop rule for the agent* moved up beside *Who does what*; the steps keep their numbers, which other files cite.
3. **Drift fixes.** `QUICKSTART.md` step 4 chooses the approval mode through setup step 6 (a key and receipts in `manual` mode, none in `enforced`); `procedures/execute.md` adds "A Done task in no milestone may be removed at once", which `AGENTS.md` already said and the named-record rule after it still bounds; the skill now says "three to four thousand words".
4. **Measure (option 1).** The S7 rule in `procedures/maintenance.md` also records the size of each procedure a change touches, against the skill's session budget.
5. **Recurrence check.** `validator/test/procedure-links.test.js` fails when a `procedures/NAME.md` named in the corpus or the scaffold, a bare `NAME.md` named in a procedure, or a *Section* named after one of them does not exist. Observed failing on four planted mutations (a renamed file in `QUICKSTART.md`, a missing section in `bin/wf-adopt` and `procedures/identity.md`, a bare file in `procedures/execute.md`), then passing. A sentence-level comparison of the old and new `procedures/execute.md` and `procedures/setup.md` found every old sentence in the new text apart from these rewordings, the pointer lines, "Claude Code, Codex and Cursor subagents start with an empty context and see only their prompt" (was "... each spawn subagents that start ... see only the prompt they are given") and **Bug fixes.** "Reproduce" (was "For bug fixes, reproduce"). It checks that pointers resolve, not that a rule survived a rewrite; that stays with the independent review.

**Limits:** no rule was shortened, so the corpus total grows. `SCHEMA.md`, `QUICKSTART.md` beyond step 4, `POLICY.md` and the other procedures are unchanged. The Done-and-remove rule still has four homes (`AGENTS.md`, `procedures/execute.md`, `procedures/readiness.md` for the planning order the owner asked for there, and `SCHEMA.md` for the check); they now agree.

**Corpus size (per the S7 rule):** IDEA-10's formula: 20,471 before (at `9cb4283`) and 20,649 after, a growth of 178 words (0.9%), all headings, pointers and the new file's opening. Per procedure: `procedures/execute.md` 1,611 to 1,329, `procedures/setup.md` 1,756 to 1,463, `procedures/shared.md` new at 694, `procedures/maintenance.md` 557 to 571.

---

## Themes across the ideas (note-taker's observation, for the evaluator to confirm or discard)

1. **Self-verification by running the app** (IDEA-02, A10, A11). This is the one change the owner explicitly asked for. It is probably the highest-value item, and the harness credential limit needs a practical workaround.
2. **A sharper task brief** (A2, A8, B2, B3, B5): explicit do-not / out-of-bounds, files to read, "list assumptions first", "present options instead of guessing". These could be small additions to `templates/task.md` and `procedures/execute.md`.
3. **Tooling and model choices** (IDEA-01, A10, B4): vet external tools through the existing toolbox process. Recommend models only if usage data justifies it.
4. **Technique selection** (IDEA-05, overlapping A2/A6/A11 and B5/B7): mostly present in substance. The likely real gaps are a test-first preference, a lighter bug-fix lane, negative examples, and evals for AI features in the product.
5. **Much of the videos' advice already exists here in stricter form** (readiness gates, independent review, stop conditions, durable records). Don't add rules that repeat what the workflow already enforces.
6. **Cost of adoption and the lifecycle boundary** (IDEA-06, IDEA-07, added 2026-09-25). The first real adoption attempt was too expensive; the fix must come from sequencing, scripting and a usage limit, not from fewer controls. The owner has since directed that the flow cover the full lifecycle; whatever is added for deployment and operation must respect the same cost concern.
7. **The owner as supervisor** (IDEA-08, added 2026-09-25). The owner expects to coordinate rather than implement. The records and validator already hold the facts; what is missing is a derived, read-only view, and above all a list of what is waiting on the owner.
8. **A lighter shell around the same rules** (IDEA-09, added 2026-09-25). The Super System template restates much of `POLICY.md` in a quarter of the words, adds owner-facing surfaces (one measure, one-page state, dashboard, inbox, portfolio), product-code guards and a naive unattended loop, but has no approval evidence, budgets or recovery. Its value here is as a design for the lite lane that IDEA-06 and IDEA-08 keep asking for, and as a source of small, concrete gates (a failing run per acceptance, a runnable verification command, rollback exercised before release).
9. **Checks, not prompts** (IDEA-11, added 2026-09-25). Quality across a growing codebase comes from the project's own required checks and from turning recurring review findings into new checks. The workflow can require those checks and record what each layer catches; it cannot supply stack-specific checkers, and the owner's approval stays an evidence check, not a code audit.
10. **More than one owner** (IDEA-13, added 2026-09-25). Two people can share a project if the shared facts are on GitHub rather than on one machine: claims as branches, IDs checked against history, what waits on whom in the status issue. Who approves what between people stays a rule the people keep, since GitHub checks only that some code owner approved.
11. **The cost of the instructions themselves** (IDEA-16, added 2026-09-26). Each change records its word count but nothing pays growth back; the corpus grew by more than a third in two days. Evaluated: a session reads a fifth of it, so the cost is findability and drift on the most-read procedures, not tokens; the answer is structure and one home per rule, not a budget.
